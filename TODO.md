# TODO — SimpleClaw 백로그 및 진행 상태

> 이 문서는 프로젝트의 백로그, 진행 중인 작업, 완료된 작업을 추적합니다.
> 새 작업 시작 전에 이 파일을 확인하고, 완료 시 상태를 업데이트하세요.

## 상태 기호

- `[ ]` 대기 (Backlog)
- `[>]` 진행 중 (In Progress)
- `[x]` 완료 (Done)
- `[!]` 블로커 (Blocked)
- `[-]` 취소/보류 (Cancelled)

---

## In Progress

- [>] **[BIZ-645](mention://issue/4e305f23-136d-48fa-896c-c7fb77fafd4d): Composer 요청에 deterministic sampling 경계를 추가** — PR #699 exact head의 BIZ-643 Guard·prompt·validator semantic 변경을 최신 `dev`에 통합하고 FinalResponseComposer에 provider-neutral request-scoped `temperature=0.0`을 적용한다. OpenAI/OpenRouter·Gemini/Vertex·Claude native mapping, CLI fail-closed, send/stream 전달, static `extra_body` 충돌 제거, effective sampling fingerprint/validator 출력과 기존 caller `None` 호환을 고정한다. replacement exact-head real KBO 3/3·CI·필수 리뷰 전에는 PR #699 merge, release, production activation, restart, Telegram 전송을 수행하지 않는다. (진행: 2026-08-08)

- [>] **[BIZ-643](mention://issue/6e01190d-8c4e-4c10-a208-9044abb05d09): production persona 자연 질문 composer gate 통과** — activation validator의 scenario-local presentation prompt와 engineered 질문을 제거하고, orchestrator와 동일한 configured SOUL-only persona assembly 및 자연 질문을 사용해 독립 3-run connected no-send gate로 교체한다. Guard는 서로 다른 projected field 사이의 일반 조사만 후속 lexical/symbol 검증에 맡기되 canonical order·중복·same-field 관계 재조합·uncited/ungrounded/top-N/unsafe 차단을 유지한다. PR #699 exact head 재검증이 2/3에 그쳐 HOLD이며, BIZ-645 replacement가 deterministic sampling 보완과 최종 3/3·CI·필수 리뷰를 소유한다. (진행: 2026-08-08)

- [>] **[BIZ-641](mention://issue/9b359807-b364-40e8-a646-81a4bcd93307): divergent composer gate를 canonical PR에 통합** — [PR #696](https://github.com/ingki3/simpleclaw/pull/696)을 canonical target으로 삼아 dynamic concrete-path enum·root-relative prompt와 scalar/top-3 connected production no-send gate를 함께 유지한다. 시나리오별 provider 1회, retry 0, Telegram/Cron adapter·ConversationStore writer·supporting dispatch per-run delta 0을 fail-closed spy로 고정하고 source 통합 커밋 이후 Graphify를 재생성한다. exact-head CI·필수 review·squash merge 전에는 production activation·restart·Telegram 전송을 수행하지 않는다. (진행: 2026-08-08)

- [>] **[BIZ-640](mention://issue/29e9c2b4-9703-4479-94ce-1d2702841d0e): projected citation path를 schema allowlist로 고정** — input별 scalar leaf만 `cited_paths` enum으로 허용하고 prefixed/wildcard/container/unknown path를 schema·Guard에서 fail-closed한다. 최종 검증·병합은 BIZ-641이 통합하는 canonical [PR #696](https://github.com/ingki3/simpleclaw/pull/696)이 소유한다. (진행: 2026-08-08)

- [>] **[BIZ-639](mention://issue/dd59d5fa-09be-4db0-9c3f-18373b44231c): 중앙 composer citation schema를 activation gate로 강화** — required non-empty citation, invalid citation fail-closed, provider 1-call/no-retry, durable generic fallback 계약을 완성했다. [PR #695](https://github.com/ingki3/simpleclaw/pull/695)의 connected sink-spy 보강은 BIZ-641이 PR #696에 통합하며, canonical merge 후 #695를 superseded로 종료한다. (진행: 2026-08-08)

- [>] **[BIZ-619](mention://issue/676488c0-4e71-4474-a585-ae564192206b): Payload-origin architecture guard의 wrapper·비제어식 우회를 차단** — BIZ-618 PR #663 merge 직전 late finding을 corrective lifecycle로 분리했다. opaque payload identity를 보존하는 `dict`·`cast`·method/module copy wrapper를 추적하고, protected Core 전체에서 return·assignment·formatting을 포함한 static-key read를 architecture violation으로 차단한다. 현재 Core false positive 0, 전체 contracts/unit/Ruff, deterministic full Graphify와 corrective PR exact-head CI·독립 Stage D PASS 전에는 merge·release·live 재배포·V4 primary 재활성화·Telegram smoke를 수행하지 않는다. (진행: 2026-08-07)

- [>] **[BIZ-618](mention://issue/a29f937a-fc6b-42de-9c75-313b832519c2): Naver Sports read-only effect metadata와 V4 typed renderer를 정렬** — production helper의 정상·empty·error·compact payload에 top-level `side_effect=false`를 명시하고, Naver Sports asset이 allowlisted 정규화 필드로 deterministic·bounded `answer`를 소유한다. Core 밖 generic presentation 경계는 typed status/effect/side-effect 안전 gate를 preferred text보다 먼저 적용하고 Core는 opaque payload 의미를 해석하지 않는다. payload alias/data-flow와 sports vocabulary의 Core 재유입을 architecture mutation guard로 차단하며 confirmation·idempotency·exactly-once 경계와 raw diagnostic 비노출을 유지한다. Corrective PR의 fresh exact-head CI와 독립 Stage D 리뷰 전에는 merge·release·live 재배포·V4 primary 재활성화·Telegram smoke를 수행하지 않는다. (진행: 2026-08-07)

- [>] **[BIZ-573](mention://issue/d91645fd-f0de-48b9-a10f-7469773f827c): LangGraph V4 release·controlled live activation·rollback 검증** — [PR #659](https://github.com/ingki3/simpleclaw/pull/659)을 main merge(SHA `6da2db10607cf5034d419f9438ab763d079f2aa1`)해 release tag `v2026.08.06.1` 산출물을 확정했다. exact main을 live checkout에 배포했고 release-deploy drain restart·health smoke·post-deploy no-send를 모두 PASS했다. 운영자 승인 후 V4 primary 전환, 승인 절차의 drain restart·health 확인, controlled Telegram query 1건의 response-source·delivery·ConversationStore exactly-once·중복 `0`, 실패 시 rollback·재기동 증거가 남아 있으므로 BIZ-573과 부모 BIZ-562는 진행 중으로 유지한다. 본 tracker 보정에서는 live config·restart·primary activation·Telegram send를 수행하지 않는다. (진행: 2026-08-07)

- [>] **[BIZ-601](mention://issue/89934d4a-1925-43bd-8c4f-ae7edf2c952b): PR #634 primary 안전 blocker를 보정** — merged `dev`의 BIZ-600 runtime에 durable invocation claim, post-dispatch fallback 차단, unsafe effect/invalid mode fail-closed, guarded user-facing composition과 validator 고정 contract gate를 추가한다. BIZ-601 수정 PR의 fresh CI와 필수 안전 리뷰 전에는 live primary activation을 재개하지 않는다. (진행: 2026-08-06)

- [x] **[BIZ-597](mention://issue/669262f3-5999-4329-8657-0c16a37c2aee): Deep Research catalog asset 안정성을 복구** — runtime-visible·declared catalog의 exact `(type, name)` identity만 validation retry에 재주입하고, full/partial Skill의 primary·supporting 위치와 미선택 Skill adapter를 안전한 read-only scope에서 deterministic하게 정규화했다. unknown/internal identity와 mutation·confirmation scope는 기존처럼 fail-closed한다. Pinned `gemini/gemini-3-flash-preview` 집중 fq-12/fq-21 6-run 및 authoritative 25-case/39-run은 모두 **GO**이며 schema 100%, route+mode+context macro 97.4%, critical pass/stability 100%/100%, connected 31/31, contract gap·inventory fidelity·rollback 0, Telegram/Cron/ConversationStore `0/0/0`이다. unit `3450 passed, 3 xfailed`, V4 integration/shadow `40 passed`, contracts `28 passed`, 변경 범위와 `src/tests` Ruff를 통과해 본 PR의 code/offline 범위를 완료했다. 후속 [BIZ-580](mention://issue/265ea749-7e47-4ea2-a6e1-226a2a12fe6d)이 merge 후 최신 `dev` 기준 최종 판정을 소유하며, `dev`의 기존 `scripts/dev/validate_langgraph_v4_no_send.py:E402`는 본 변경과 분리한다. live config·restart·deploy·사용자-visible 전송은 수행하지 않았다. (완료: 2026-08-06)

- [x] **[BIZ-596](mention://issue/a0fff3e0-b8fb-481a-82e8-62273cae69d1): full Skill 정규화의 mutation scope를 보존** — full-coverage Skill의 중복 scope를 좁힐 때 adapter와 supporting collector에 정확히 대응하는 runtime-visible declared read-only tool만 제거하도록 제한했다. mutation/confirmation-required tool은 원본 scope와 confirmation gate에 보존하고, unrelated read-only 및 unknown/internal tool은 boundary에서 fail-closed한다. source `cb3854a`에서 첨부 최소 재현, turn planner 26건, 전체 unit `3444 passed, 3 xfailed`, V4 integration/shadow `111 passed`, contracts `28 passed`, 변경 범위 Ruff를 통과했으며 Graphify는 14,910 nodes/32,436 edges로 갱신했다. exact parent head의 기존 scripts E402는 본 변경 범위에서 분리해 기록했다. [PR #629](https://github.com/ingki3/simpleclaw/pull/629)는 `dev`에 squash merge(SHA `0f31349aceee9a02c0d7375082652cccdc2dfd70`)됐고 live config·restart·deploy·사용자-visible 전송은 수행하지 않았다. (완료: 2026-08-06)

- [x] **[BIZ-592](mention://issue/430addc5-ccde-4dd7-89d5-0555cdaac56e): Connected probe가 composed asset plan을 검증하게 개선** — contract-complete primary/supporting 자산을 exact identity의 deterministic 순서로 immutable single-primary plan에 투영해 모두 connected shadow graph로 검증하고, stop·rollback·Telegram/Cron/ConversationStore side effect를 fail-closed 집계한다. 자연어 질문은 contract 검증 성공 시 그대로 사용하며 불일치 시 asset schema가 선언한 `default`/`examples`만 허용하고 미선언 fallback은 명시적으로 중단한다. supporting-only `web_search`+`web_fetch`, CLI Skill declared example·missing example, 일부 실패·stable ordering 회귀를 추가했다. BIZ-591/BIZ-593 production contract를 포함한 최신 `dev`를 [PR #624](https://github.com/ingki3/simpleclaw/pull/624)에 최종 통합했고 결합 head에서 V4 integration `16 passed`, contracts `28 passed`, 전체 unit `3436 passed, 3 xfailed`, Ruff와 CI 3종을 통과했다. Graphify는 source `9fa4e9b` 기준 14,884 nodes/32,364 edges로 재생성했고, 최종 exact head `a253293`의 독립 Stage D focused `42 passed`와 CI 3종을 재대조한 뒤 [PR #624](https://github.com/ingki3/simpleclaw/pull/624)를 squash merge(SHA `2dc4b4c8be0d2cc1da04bc6e25da2c362625181c`)했다. live dispatch·restart·deploy는 수행하지 않았다. (완료: 2026-08-05)

- [x] **[BIZ-593](mention://issue/7df3acdd-96a8-4e24-b17f-b60a503b5bdf): Registry contract example과 shopping Skill 계약을 보강** — production Skill 6개의 input schema에 canonical example을 선언하고 `naver-shopping-skill`의 read-only owner/ref/schema/binding/example을 외부 skills 저장소와 repository-owned fixture에 함께 고정했다. CI는 registry metadata에서 example을 직접 읽어 canonical validation하고 missing/invalid example, shopping mutation-shaped args, Recipe→naver-sports binding mismatch를 fail-closed로 차단한다. 외부 skills [PR #3](https://github.com/ingki3/skills/pull/3)을 squash merge(SHA `e08ee70fb643c9056c75c788c6d0e2ed6975a67b`)한 뒤 exact-head CI와 독립 리뷰를 통과한 SimpleClaw [PR #626](https://github.com/ingki3/simpleclaw/pull/626)을 squash merge(SHA `2fc9c2b69777d3a40aaa62df0a9da9b049a4b6c9`)했으며 live install·restart·배포는 수행하지 않았다. (완료: 2026-08-05)

- [x] **[BIZ-591](mention://issue/5d932fd8-2d22-4d1e-bfc5-847c9750659d): Production skill owned contract v2를 완성** — `us-stock-skill`·`gmail-skill`·`naver-sports-skill`의 owner-qualified read-only contract/binding과 `google-calendar-skill`의 confirmation-required mixed-operation contract를 production skills [PR #2](https://github.com/ingki3/skills/pull/2)에 versioning했다. discovery→catalog→registry owner/ref/hash/binding, sports-live delegate target, metadata drift dispatch 0, 새 read-only connected completion과 Calendar 자동 dispatch 거부를 검증했다. 외부 PR을 squash merge(SHA `a48dafd0f04807edaa2d4cd944122a8993930f77`)한 뒤 exact-head CI와 독립 리뷰를 통과한 SimpleClaw [PR #625](https://github.com/ingki3/simpleclaw/pull/625)를 squash merge(SHA `fd1eaa612564f26f4b0b1d2f0736436b507e57a7`)했으며 live install·restart·배포는 수행하지 않았다. (완료: 2026-08-05)

- [x] **[BIZ-586](mention://issue/5e7d3258-55e5-4fd2-9a66-b08d9baf612f): V4 scenario evaluator의 phase attribution을 분리** — planner/schema, PlanGate, contract classification, connected execution, score 예외 경계를 분리하고 raw exception text를 폐기한 phase별 code를 보고한다. Connected `ValueError`/`TypeError`는 planner schema validity·usage를 보존한 명시적 HOLD로 기록하며 consecutive/total schema early-stop에는 실제 planner/schema 실패만 포함한다. Side-effect·budget·deadline 즉시 fail-closed 정책을 유지했고, exact-head CI 3종과 독립 focused 회귀 `33 passed`를 확인했다. live config·restart·deploy·사용자-visible 전송 없이 [PR #618](https://github.com/ingki3/simpleclaw/pull/618)을 squash merge(SHA `b74844ef4792f6d1eac3dcd82d4c4241db5676a7`)했다. (완료: 2026-08-05)

- [x] **[BIZ-579](mention://issue/94865969-6d6c-471d-b6cd-602ecd4e72b3): Production asset contract coverage gap을 보강** — `google-news-search-skill`·`kr-stock-skill`의 owner-qualified read-only 계약을 skills 저장소 [PR #1](https://github.com/ingki3/skills/pull/1)에 versioning하고, SimpleClaw CI는 repository fixture에서 두 skill과 `web_fetch`·`web_search`의 complete contract 및 connected synthetic completion을 설치 경로와 무관하게 fail-closed 검증한다. latest `dev` 통합, source 이후 Graphify provenance, 독립 focused `19 passed`, exact-head CI 3종을 확인해 SimpleClaw [PR #615](https://github.com/ingki3/simpleclaw/pull/615)를 squash merge(SHA `2c60875cb03f8ee64e01b16f86b6699891c64efe`)했다. production skills merge SHA는 `46837df53f76131695ed9fe3a29cefe9719cbceb`이며 mutation/side-effect dispatch 0과 Telegram/Cron/ConversationStore `0/0/0`을 유지한다. (완료: 2026-08-05)

- [x] **[BIZ-581](mention://issue/b1190fec-df9e-4608-9fa5-15c237d70da3): Clarification과 context binding 안전성을 강화** — mutation confirmation과 ambiguous follow-up을 dispatch 전에 fail-closed로 중단하고 selected turn relation·mode binding을 고정했다. fq-25 confirmation pre-dispatch interrupt, fq-22/30/31/32 fixed-gold gate, fq-23 attachment scope gap not-scored를 확인했으며 mutation callback과 evaluator의 Telegram/Cron/ConversationStore 호출은 각각 `0`과 `0/0/0`이다. source `b41265fe604bfe5bf58d3e28f3739f93186c4c12`에서 focused `46 passed`, 전체 unit `3418 passed, 3 xfailed`, V4 통합 `28 passed`, Ruff와 exact-head CI 3종을 통과한 [PR #614](https://github.com/ingki3/simpleclaw/pull/614)의 code/offline 구현 범위를 완료했다. 실행 의미 파일의 추가 변경 없이 lifecycle과 Graphify provenance만 보완하며 merge 판정은 독립 Stage D가 소유한다. (완료: 2026-08-05)

- [x] **[BIZ-582](mention://issue/f8e777c7-8b3a-481a-b5c5-051bca70aba6): Planner schema boundary 오류를 해소** — source `8184d0d`에서 runtime-visible native tool 이름을 provider schema enum으로 제한하고, exact Recipe의 신뢰된 `skills` binding에 직접 연결된 안전한 delegate만 중복 scope에서 제거하도록 provenance를 고정했다. unrelated read-only Skill은 `unknown_or_internal_tool`로 fail-closed하며, 유효한 catalog scope의 confirmation 누락은 `false→true`로만 보정한다. BIZ-581 결합 focused `87 passed`, 전체 unit `3425 passed, 3 xfailed`, source Ruff를 통과했고 Graphify는 14,801 nodes/32,081 edges로 current-base 기준 재생성했다. (완료: 2026-08-05)

- [x] **[BIZ-583](mention://issue/44175ec4-b9f4-4436-b359-f26becc3b92d): V4 시나리오 benchmark ingress fidelity를 수정** — PR #613의 preliminary 42.0%에서 production ingress, runtime/operator scope, attachment 부재, synthetic contract probe provenance를 분리했다. 32개 inventory를 `25 runtime-scored / 7 not-scored`로 보존하고 corrected 39-run schedule을 생성했으며, actual provider는 4/64 calls에서 schema failure 3연속 안전 중단되어 `evaluated 4/39`, `quality_evaluation_complete=false`, HOLD, Telegram/Cron/ConversationStore `0/0/0`을 authoritative evidence로 기록했다. focused 22건, V4 scenario+shadow 28건, contracts 15건, 전체 unit `3414 passed, 3 xfailed`, 변경 범위 Ruff를 통과했고 Graphify를 source `bd4cec057e968dbfba114c6f70acc0eaf4448870` 기준 14,777 nodes/31,976 edges로 갱신했다. runtime routing·live config·restart·사용자-visible 전송은 변경하지 않았다. (완료: 2026-08-05)

- [x] **[BIZ-578](mention://issue/06b77863-21f0-48f2-aba7-bcbb036bc14e): LangGraph V4 반복질문 시나리오를 평가** — PR #613의 32-case fixed-gold/actual-provider evaluator에 critical gold pass, 자산별 exact identity contract 검사, 필수 connected completion과 rollback-free GO gate를 추가해 Stage D HOLD의 merge-blocking 결함을 fail-closed로 보완했다. supporting/allowed asset 누락·불완전·mutation 및 stability 1.0인 오답 critical 회귀를 고정했고 focused `19 passed`, 전체 unit `3411 passed, 3 xfailed`, Ruff를 통과했다. Graphify는 source `51340d3b4b337bc508311e4cf252aa56dd4b7bb9` 기준 14,768 nodes/31,949 edges로 갱신했다. actual routing 품질은 BIZ-579~582가 별도 소유하며 live primary 활성화·재시작·사용자-visible 전송은 수행하지 않았다. (완료: 2026-08-05)

- [x] **[BIZ-577](mention://issue/46f27f8a-65db-47de-9bd3-9338465c4835): Shadow parallel budget를 실제 동시성 슬롯으로 수정** — source `908da37545daa0988aaebc9340ed4d94bf565291`에서 historical `parallel_peak` lifetime exhaustion gate를 제거해 slot 해제 뒤 순차 graph/asset 작업을 허용하고, active slot 점유 중 추가 asset/LLM 시작은 fail-closed로 차단하며 high-water telemetry를 유지했다. 검증 head `708583c288bdf217b31058f0aa8b156b3d28755b`에서 focused·contracts·actual-provider 3-way·fresh CI를 통과해 본 자식의 code/offline remediation 범위를 완료했다. 공유 PR #609 머지와 부모 Stage D 완료 판정은 [BIZ-572](mention://issue/44a03491-e8c7-4352-875d-72a1bcaf723e)가 소유하며 본 완료 표기는 해당 머지 승인이 아니다. (완료: 2026-08-05)

- [x] **[BIZ-572](mention://issue/44a03491-e8c7-4352-875d-72a1bcaf723e): LangGraph V4 shadow/no-send observability와 canary gate 구현** — ordinary production turn의 exact planner→PlanGate 결과를 설정 기반 V4 facade→격리 checkpoint→Recipe-first graph→Null delivery→ConversationStore persistence 경계→allowlisted telemetry→rollback/read-only canary 판정까지 연결했다. Telegram/ConversationStore/Cron notifier 실제 callback을 per-run 계측하고 graph/asset/LLM/token/parallel/deadline budget을 pre-start에서 강제했으며, BIZ-577에서 parallel slot release 회귀까지 보완했다. Exact head actual-provider Recipe/ReAct/DeepResearch 3-way, contract continuity `4/4`, 외부 callback `0/0/0`, stop `completed`, rollback `false`, unit `3396 passed, 3 xfailed`, contracts `15 passed`, Ruff와 CI 3종을 통과해 code/offline no-send 범위를 완료했다. Live config 변경·primary/canary 활성화·restart·사용자-visible send는 후속 [BIZ-573](mention://issue/d91645fd-f0de-48b9-a10f-7469773f827c)에서 별도 승인·검증한다. (완료: 2026-08-05)

- [x] **[BIZ-571](mention://issue/e39cf4ad-70c1-4877-83b1-86826ee1b822): Domain-neutral Recipe/Skill contract extension fixture 추가** — 임의 key의 repo fixture로 Recipe→Skill deterministic binding, canonical payload continuity, identity·schema·definition·binding drift 및 1-byte payload mutation dispatch 0을 검증하고, arbitrary payload-key control-flow·static contract ID·asset import·evidence reducer의 Core 재도입을 막는 AST 계약을 추가했다. 두 차례 Stage D HOLD 보완 뒤 focused `55 passed`, 전체 unit `3395 passed, 3 xfailed`, Ruff 0.16.1과 exact-head CI 3종을 통과한 [PR #604](https://github.com/ingki3/simpleclaw/pull/604)를 `dev`에 squash merge(SHA `fd40f729f9776d450567b51a0ac5f6eda6b0352e`)했다. Runtime Core source, live asset/config/provider/model route/Telegram 설정은 변경하지 않았다. (완료: 2026-08-05)

- [>] **[BIZ-562](mention://issue/7806f941-2690-4d72-944f-1ca1e2c46b13): LangGraph V4 domain-neutral runtime 도입** — 계약·ledger·Recipe-first core·adapter·final composition/delivery/persistence의 code/offline 자식 구현을 순차 검증 중이다. BIZ-570 완료 뒤 [BIZ-572](mention://issue/44a03491-e8c7-4352-875d-72a1bcaf723e)가 shadow/no-send observability와 canary gate를, [BIZ-573](mention://issue/d91645fd-f0de-48b9-a10f-7469773f827c)가 release·controlled live activation·rollback을 소유한다. 따라서 부모 완료와 live 전환은 BIZ-573 검증 전까지 진행 중으로 유지한다. (진행: 2026-08-05)

- [x] **[BIZ-576](mention://issue/7f60e093-3cb1-4f77-9c15-2d0a0583803b): dev integration의 stale provider·planner fixture를 현행 계약에 맞게 복구** — 부모 [BIZ-572](mention://issue/44a03491-e8c7-4352-875d-72a1bcaf723e) 검증에서 clean `origin/dev`에도 재현된 base-red 7건을 별도 code/offline 범위로 분리했다. 현행 provider protocol과 unified planner·typed realtime lookup 계약에 fixture를 정렬해 focused `7 passed`, 전체 integration `145 passed, 6 skipped`, 전체 unit `3395 passed, 3 xfailed`, Ruff, exact-head CI 3종 및 독립 Stage D 재검토를 통과한 [PR #610](https://github.com/ingki3/simpleclaw/pull/610)을 `dev`에 squash merge(SHA `6a1d26bf4d56e4fd0843770744de45b7aa1ac999`)해 본 이슈의 code/offline 범위를 완료했다. production 코드와 BIZ-572의 shadow/no-send telemetry·canary gate는 변경하지 않았다. (완료: 2026-08-05)

- [x] **[BIZ-570](mention://issue/70b6423f-88b6-4731-81d4-877e078a2fb2): Final composition, response guard, delivery journal과 persistence 구현** — Recipe-first production graph의 `compose_candidate → final_composition → prepare_delivery → commit_delivery → persist_delivery_outcome → END` 경로에 startup/channel/memory callbacks를 주입하고, sender receipt에 external identity가 없으면 `UNKNOWN` terminal로 fail-closed하며 automatic resend와 ConversationStore write를 차단했다. SQLite delivery journal, delivery outcome별 persistence, guard 전 artifact/intent 부재와 guard 실패 뒤 asset redispatch 0을 connected graph 회귀로 검증했다. fresh CI 3종과 독립 Stage D 재검토를 통과한 [PR #605](https://github.com/ingki3/simpleclaw/pull/605)를 `dev`에 squash merge(SHA `3a7b5bea3a258f4db3d6de45a82954fb369e8dd7`)해 본 이슈의 code/offline 범위를 완료했다. live sender/config/restart는 수행하지 않았으며 shadow/no-send는 BIZ-572, release·live activation·rollback은 BIZ-573이 소유한다. (완료: 2026-08-05)

- [x] **[BIZ-569](mention://issue/fcad1af5-3a02-4a19-8d1e-3f586683d7eb): LangGraph V4 Cron compatibility와 retry ownership 구현** — Cron ingress/result mapping, scheduler·solver·delivery retry ownership 분리, checkpoint DB 격리와 live notifier fail-closed 경계를 구현했다. focused Cron/daemon `69 passed`, 전체 unit `3382 passed, 3 xfailed`, Ruff와 exact-head CI 3종 및 독립 Stage D 재검토를 통과한 [PR #603](https://github.com/ingki3/simpleclaw/pull/603)을 `dev`에 squash merge(SHA `2bac0a4049b082d77874d02492cde05e8a7c1345`)했다. release/live 활성화·운영 config 변경·restart·사용자-visible 외부 전송은 별도 후속 이슈 범위로 유지한다. (완료: 2026-08-05)

- [x] **[BIZ-568](mention://issue/2c0854b8-ee99-4c36-a156-136805d567c6): Generic Recipe/Skill adapters와 deterministic binding 구현** — domain-neutral Recipe/Skill adapter와 owner-owned deterministic binding, invocation signature·payload hash·action receipt 기반 redispatch guard를 구현했다. executor 실패-result는 `executor.failed` fail-closed 응답으로 정규화하고 side-effect 실패를 `BLOCKED + UNKNOWN + dispatched=true`로 보존하며, 동일 invocation의 후속 dispatch는 executor 재호출 없이 차단한다. task-owned 한국어 docstring·판단 주석과 실패 후 redispatch 0 회귀를 보강하고 Graphify manifest를 source remediation SHA `4728127a7e38d31f108a1415bdc4c5cb96fe8eb2` 기준으로 갱신했다. 전체 unit `3357 passed, 3 xfailed`, Runtime Contracts `10 passed`, Offline Integration `46 passed, 36 deselected`, Ruff와 exact-head CI 3종 및 독립 Stage D 재검토를 통과한 [PR #598](https://github.com/ingki3/simpleclaw/pull/598)을 `dev`에 squash merge(SHA `0bd434ca3b332f0349be717fba5eba61a9b910b8`)했다. live config/restart/외부 전송은 후속 release/live 이슈 범위로 유지한다. (완료: 2026-08-05)

- [x] **[BIZ-565](mention://issue/c8b5312b-4d0f-41a7-a252-220680b5f75d): LangGraph V4 event ledger와 single-writer reducer 구현** — immutable event/action/delivery receipt와 typed snapshot, `reduce_state` 단일 writer, legal transition·terminal lattice를 구현했다. Stage D HOLD에서 확인된 effect lifecycle, idempotency key uniqueness, route remaining-budget 상속·검증을 보정하고 fresh receipt replay, same-key conflict, budget mismatch 회귀를 추가했다. 독립 focused `17 passed`, 결합 회귀 `72 passed`, adversarial reducer probe, Ruff 0.16.1 및 exact-head CI 3종을 통과한 [PR #595](https://github.com/ingki3/simpleclaw/pull/595)를 `dev`에 squash merge(SHA `91f6ec5694b0ba0d052301a3f7f330d61fe66d66`)했다. live config/restart/외부 전송은 후속 release/live 이슈 범위로 유지한다. (완료: 2026-08-05)

- [x] **[BIZ-566](mention://issue/014d8f00-a645-49de-98b5-aa69a07b06bd): Discovery-built Recipe/Skill Contract Registry 구현** — Skill/Recipe 소유 owner-qualified schema와 deterministic binding metadata를 discovery하고, Core의 static contract map이나 asset-specific import 없이 immutable definition/registry/catalog fingerprint로 고정했다. malformed schema와 owner/schema/definition/binding/registry drift를 fail-closed 처리하면서 legacy asset discovery와 V4 metadata opt-in을 유지했다. 전체 unit `3328 passed, 3 xfailed`, Ruff 0.16.1 `src/ tests/ scripts/`, exact-head CI 3종과 독립 Stage D 재검토를 통과한 [PR #596](https://github.com/ingki3/simpleclaw/pull/596)을 `dev`에 squash merge(SHA `2372a3d995e5d7f5b625f7ac484dea75776831fa`)했다. live config/restart/외부 전송은 후속 release/live 이슈 범위로 유지한다. (완료: 2026-08-05)

- [x] **[BIZ-567](mention://issue/89fb8bc9-24a3-4c1b-bd02-de671c97ec51): Recipe-first 3-way LangGraph V4 core graph 구현** — Recipe-first 평가와 Simple/ReAct/DeepResearch 3-way selector, exact checkpoint interrupt/resume, exhaustive terminal routing을 구현했다. Stage D HOLD에서 확인된 zero-budget DeepResearch dispatch, 중첩 observation snapshot mutation, forged clarification choice, compiled graph 통합 matrix를 보정하고 Graphify manifest를 source remediation SHA `5a57b4d8a4b56e65e41109ea377498587c33aa25` 기준으로 갱신했다. focused `58 passed`, 전체 unit `3382 passed, 3 xfailed`, Ruff 0.16.1, exact-head CI 3종과 독립 Stage D 재검토를 통과한 [PR #600](https://github.com/ingki3/simpleclaw/pull/600)을 `dev`에 squash merge(SHA `5db6ac5ca106d4e35cea9dfff605af4a02b96685`)했다. live rollout·운영 asset migration·외부 전송은 후속 release/live 이슈 범위로 유지한다. (완료: 2026-08-05)

- [x] **[BIZ-561](mention://issue/acfd688e-a0c2-4165-865e-942320da5012): 실행 SkillDefinition과 trusted safety snapshot을 원자적으로 검증** — exact recipe scope 생성 시 discovery와 actual dispatch registry의 동일 `SkillDefinition` 객체·canonical fingerprint를 대조하고, ToolGate가 dispatch 직전 registry 삭제·교체·내용 변경을 재검증한 뒤 검증된 definition 객체를 executor까지 전달하도록 보정했다. helper 결과는 명시적 `side_effect=false`만 verified no-effect로 인정하며 true/missing/partial은 top-level `AssetResult`와 common validator에서 unknown/partial action으로 보존해 completed/resolved/allow-final 승격을 차단한다. raw planner→PlanGate→CapabilityExecutor→production exact recipe→ToolGate→actual registry→helper→validator connected 회귀와 mutation 행렬을 추가했고, 구현 exact head `c4f9760e7176e8da0eec8e5b840e406521c90f79`에서 CI 3종과 독립 safety review(변경 integration/unit 7개 `178 passed`)를 통과해 code/offline hotfix 범위를 완료했다. `capability_first_v3` live 활성화·restart·외부 전송은 본 이슈의 out-of-scope이므로 수행하지 않았다. (완료: 2026-08-03)

- [x] **[BIZ-560](mention://issue/5d71f6c7-122d-42b5-bd75-c3e85da6ba27): 설치된 sports skill capability를 runtime discovery에 연결** — version-controlled installer의 `SKILL.md` frontmatter에 production `sports-live`와 동일한 safe read-only/no-side-effect/no-confirmation capability 및 `query.v1 → asset_result.v1` typed contract를 선언했다. 실제 installer→filesystem→`discover_skills()`→production recipe→ToolGate→validator connected 경로에서 positive, undeclared/write/side-effect/confirmation/identity mismatch와 missing claim/provenance/empty/error를 고정해 본 이슈의 code/offline 범위를 완료했다. release 뒤 actual installed-asset finite-budget no-send 3/3과 live activation/restart는 [BIZ-540](mention://issue/9c2b8fc0-af9f-4884-9eb7-cfba1a426cda)이 별도 gate로 소유하며, 본 child에서는 live config 변경·restart·외부 전송을 수행하지 않는다. (완료: 2026-08-03)

- [x] **[BIZ-559](mention://issue/154a8ff5-fd9b-4f52-b22d-379d0d095a5c): fast-final의 skill capability 안전 경계를 강제** — exact nested scope에 실제 discovered `SkillDefinition.capability`에서 파생한 trusted safety snapshot을 전달하고, asset identity 일치와 declared read-only/no-side-effect/no-confirmation을 ToolGate·deterministic final·CapabilityExecutor 경계에서 fail-closed로 강제했다. raw planner output부터 PlanGate→CapabilityExecutor→recipe→ToolGate→fake helper→common validator까지 동일한 연결 경로에서 safe positive, undeclared/write/side-effect/confirmation, missing claim/provenance, empty/error 회귀를 모두 고정해 본 이슈가 소유한 code/offline 검증을 완료했다. release 뒤 actual-provider no-send와 live activation/restart는 [BIZ-540](mention://issue/9c2b8fc0-af9f-4884-9eb7-cfba1a426cda)이 별도 소유한다. (완료: 2026-08-03)

- [x] **[BIZ-557](mention://issue/75867545-877a-47c8-b7d0-7fb98062dc16): typed sports recipe final envelope를 결정적으로 보존** — exact sports planner의 bounded completed-result claim을 provider canonical `game_result|score|winner` key로 정규화하고, 알 수 없는 claim은 원문 그대로 남겨 fail-closed한다. 성공한 typed helper observation의 단일 dispatch·provenance 보존과 code/offline 검증을 완료했다. live activation/restart/외부 전송은 [BIZ-540](mention://issue/9c2b8fc0-af9f-4884-9eb7-cfba1a426cda)이 소유한다. (완료: 2026-08-03)

- [x] **[BIZ-558](mention://issue/3de55bf9-3b3a-4691-9699-3c295ecf8cea): 20초 budget 안에서 sports observation을 즉시 확정** — exact read-only recipe의 단일 helper가 완전한 typed claim map과 provenance를 반환하면 추가 모델 finalization 없이 `asset_result.v1`을 결정적으로 생성한다. `어제(YYYY-MM-DD) KBO … 결과 및 스코어` supported-only 변형은 canonical claim으로 정규화하고, 미지원 잔여 의미와 불완전 observation은 fail-closed한다. production-like no-send 3/3에서 각 3.6–4.9초, helper dispatch 1회, 추가 finalization·generic limitation·외부 sink 0을 확인했다. live activation/restart/외부 전송은 [BIZ-540](mention://issue/9c2b8fc0-af9f-4884-9eb7-cfba1a426cda)이 소유한다. (완료: 2026-08-03)

- [>] **[BIZ-556](mention://issue/290b839e-e35d-44be-9049-08cfe568b87e): 전일 스포츠 결과 조회와 evidence 보존 경계 보정** — Naver structured `STARTED` live와 `ENDED|RESULT` completed result를 typed mode로 분리하고, evidence-required asset-0 plan의 structural catalog repair 및 scope cap 이후 첫 observation 보존을 구현한다. Release/live 재활성화는 구현·CI·독립 Review 이후 부모 BIZ-540의 별도 gate가 소유한다. (진행: 2026-08-03)

- [x] **[BIZ-553](mention://issue/02afa7d1-c7a3-4a4f-a174-658386931d56): Partial terminal 뒤 남은 evidence gap 전파** — 첫 supporting asset이 일부 claim을 해결하고 read-only `FAILED_TERMINAL`로 끝나도 다음 distinct allowlisted asset에 최신 질문과 남은 gap을 전달하도록 보정했다. 두 번째 attempt signature의 `selected_gap`이 남은 claim을 사용하고 해결된 claim을 재조사하지 않는 mutation-resistant 회귀를 추가했다. focused·integration `27 passed`, 전체 unit `3217 passed, 3 xfailed`, Ruff와 CI 3종(Unit Tests / Runtime Contracts / Offline Integration) SUCCESS를 확인했고, 필수 Review PASS 후 [PR #579](https://github.com/ingki3/simpleclaw/pull/579)를 `dev`에 squash merge(SHA `40a2270363032aea79a2b9306c9c264667d1124a`)했다. 부모 [BIZ-552](mention://issue/55f9b7c2-db10-47ff-8e60-ac8fae39f555)가 deterministic/actual-provider gate 재검증을 소유한다. (완료: 2026-08-03)

- [x] **[BIZ-551](mention://issue/672561b8-689f-4434-89db-8c750a434e5f): 읽기 전용 failed_terminal을 bounded fallback으로 전환** — read-only asset의 `FAILED_TERMINAL`과 Goal `BLOCKED`를 분리하고, original goal·required claims·기존 ledger evidence를 보존한 채 계획에 명시된 supporting asset만 budget 내 순환한다. terminal asset과 동일 signature는 재실행하지 않으며 denied, side-effect unknown/partial, deadline/budget 경계와 no-scope limited response를 유지한다. deterministic focused·integration 26개와 전체 unit `3216 passed, 3 xfailed`, Ruff를 통과했고 Graphify code-only 산출물은 13,640 nodes/27,562 edges로 갱신했다. 필수 Review PASS 후 [PR #578](https://github.com/ingki3/simpleclaw/pull/578)을 `dev`에 squash merge(SHA `62e83f9ee249ea91dd4c00ea6a56d112531d0426`)했다. exact merge SHA 독립 검증은 [BIZ-552](mention://issue/55f9b7c2-db10-47ff-8e60-ac8fae39f555)가 소유한다. (완료: 2026-08-03)

- [x] **[BIZ-549](mention://issue/3a9750aa-889d-45d5-8ddd-290a0e207ef4): Exact recipe runtime-visible tool scope 우회 차단** — safe typed `full_coverage` recipe의 evidence owner narrowing을 빈 top-level tool scope 또는 중복 `execute_skill`에만 허용하고, 다른 runtime-visible tool scope가 남으면 PlanGate가 repair로 fail-closed하도록 보정했다. production `plan_turn_with_llm → PlanGate` adversarial 회귀를 추가하고 독립 focused `48 passed`, 전체 unit `3202 passed, 3 xfailed`, Ruff, CI 3종을 모두 통과했으며 Graphify code-only provenance는 13,625 nodes/27,476 edges로 확인했다. 필수 Review PASS 후 [PR #575](https://github.com/ingki3/simpleclaw/pull/575)를 `dev`에 squash merge(SHA `25e5dad563b7fe27aa5662762617a1621c71ce6d`)했다. BIZ-547 PR #572의 late finding follow-up이며 release/live activation은 수행하지 않았다. (완료: 2026-08-03)

- [x] **[BIZ-547](mention://issue/50050c70-6c5e-47db-a43a-dde29340c91f): Actual-provider planner output-cap 재현성을 보정** — `finish_reason=length` 실패에서 completion 2,029 tokens 중 reasoning 1,916 tokens가 2,048 cap을 잠식했고, provider-neutral `budget_tokens=512`가 OpenRouter `reasoning.max_tokens`로 변환되지 않던 원인을 확정했다. budget mapping과 cap 전용 동일-route `minimal` reasoning 1회 복구, safe typed exact recipe의 asset evidence-owner narrowing을 [PR #572](https://github.com/ingki3/simpleclaw/pull/572)에 반영했다. unknown/unrelated/confirmation 및 두 번째 실패는 fail-closed로 유지한다. 전체 unit `3200 passed, 3 xfailed`, Ruff PASS, 동일 LPGA actual-provider Planner/PlanGate 3/3 PASS, exact recipe strict `asset_result.v1`·helper 성공 trace 정확히 1회 3/3, dispatch/persist/notifier/delivery/Telegram send 0을 확인했다. Graphify code-only update는 source commit `43a97237274a318686bc5d43a113a1a5c0118d6a`, 13,624 nodes/27,468 edges다. [PR #572](https://github.com/ingki3/simpleclaw/pull/572)를 `dev`에 squash merge(SHA `271a5bd641700915f27df59698b28e5ebeab8e8f`)했다. (완료: 2026-08-03)

- [x] **[BIZ-545](mention://issue/600c651b-b7ef-4b8c-96fd-bfd7c881ac1a): Actual-provider sports exact 경로를 3/3 결정적으로 보정** — 안전한 typed `full_coverage` recipe가 소유한 중복 top-level `execute_skill`만 빈 allowlist로 축소하고, exact recipe delegate 실행 뒤 provider final에 strict `asset_result.v1` structured-output 계약을 전달한다. unknown/unrelated/confirmation 경계와 단일 성공 delegate invariant는 fail-closed로 유지하며, [PR #569](https://github.com/ingki3/simpleclaw/pull/569)를 `dev`에 squash merge(SHA `4b3b1858a388cd53a202735e084b8e0a10a58480`)했다. live 설치·재시작·actual-provider no-send 3/3 재검증과 release/live activation은 부모 BIZ-541의 Review gate가 소유한다. (완료: 2026-08-03)

- [x] **[BIZ-544](mention://issue/442a75a3-68d5-4510-9f9d-92c69b252962): Exact primary asset allowlist 경계를 capability 계약과 정합화** — capability-native full primary를 supporting fallback과 독립적으로 runtime catalog 검증하고, legacy execution-only allowlist 호환 경계와 unknown/internal/confirmation fail-closed를 보존했다. LPGA `sports-live` canonical plan의 Planner→PlanGate→exact recipe 회귀 및 Graphify 산출물을 갱신하고, PR #566을 `dev`에 squash merge(SHA `703e8395c835a9a16f290d3f522e980b61c59c98`)했다. (완료: 2026-08-03)
- [>] **[BIZ-541](mention://issue/a3cfc463-e4f6-4929-a38f-2015e79bb23f): Naver Sports exact asset 계약을 Capability-first v3와 정합화** — [PR #563](https://github.com/ingki3/simpleclaw/pull/563)의 코드·오프라인 검증 범위는 exact delegate trace 보정 head `2f73a3a03b5debc7fbc3ff144f0a375c504006ac`까지 반영되어 최종 Review와 `dev` merge를 기다린다. merge 뒤 BIZ-541이 live `sports-live` recipe의 timestamp backup·SHA-256·rollback 경로 생성과 contract 설치를 소유하고, 이후 부모 [BIZ-540](mention://issue/9c2b8fc0-af9f-4884-9eb7-cfba1a426cda)이 `capability_first_v3` activation·restart·no-send smoke를 재개하므로 아직 완료로 전환하지 않는다. (진행: 2026-08-02)

- [x] **[BIZ-538](mention://issue/2e969590-74fa-4f97-9971-488bb9d58d4c): Dreaming backend alias를 live router와 정합화** — PR #560을 `dev`에 squash merge(`7d854bf8bd2dda1c08c75e75a9bffd0a0006773c`)하고 live `daemon.dreaming.model` 한 필드를 `openrouter_gemini_3_6_flash`로 변경했다. timestamp backup/rollback을 보존한 채 PID `43020`으로 drain/restart했고 Admin·Telegram·Webhook·Scheduler health와 실제 Dreaming run 1회 success(19:06:24~19:07:20 KST, input 22건)를 확인했다. 이후 두 개의 5분 scheduler 관측 창에서 신규 ERROR/CRITICAL/Traceback 및 Telegram send API가 0이었고, 두 번째 실제 run은 daily guard(`오늘 이미 1회 실행됨`, next run 03:00)로 N/A다. [BIZ-539](mention://issue/19b1404f-ed4e-4965-897c-5c17e03491ea) alias remediation도 `done`이다. (완료: 2026-08-02)

- [>] **BIZ-17: LLM 토큰 사용량·비용 추적 및 임계 경보** — 실제 provider attempt 단위의 content-free usage event, versioned backend pricing 기반 micro-USD 예상 비용, 별도 SQLite 일/월 집계와 durable alert claim, dashboard API/panel, alert-only Telegram 배선을 구현한다. production 가격·threshold·enable/restart는 운영자 승인 전 적용하지 않는다. (진행: 2026-08-02)

- [>] **BIZ-525: Capability-first Goal Resolution과 4-mode 실행 구조** — full-coverage exact Skill/Recipe를 mode 이전에 typed `asset_result.v1`로 실행하고 Asset 상태와 Goal 상태를 분리한다. 미해결 gap은 bounded evidence/complex controller로 전환하며 모든 경로를 공통 Evidence/Action Validator에 합류시킨다. 기본 architecture는 `legacy_v2`로 유지하고 유한 budget이 설정된 `capability_first_v3`만 명시 활성화한다. live rollout/restart는 별도 운영 승인 범위다. (진행: 2026-08-02)
- [x] **[BIZ-523](mention://issue/8f9d3edd-4755-43ba-9d7f-222aeaf52dbd): Turn/Session/Evidence typed state 통합 — 코드·release·live rollout 완료** — [PR #537](https://github.com/ingki3/simpleclaw/pull/537)을 `dev`에 squash merge(SHA `d5005bddeb6ceba9a07e6a92b75c2858c06c61e7`)하고, late-finding 보정 [BIZ-529](mention://issue/c3d46b6a-cc05-4d42-b2fb-b1c3c87e957c)의 [PR #544](https://github.com/ingki3/simpleclaw/pull/544)를 squash merge(SHA `e0f2bf411bc239d402ee9ac5785547443c47701e`)한 뒤, 후속 정책 보정 [BIZ-531](mention://issue/da36b01c-94a0-44df-be39-46a7b9c91f54)의 [PR #546](https://github.com/ingki3/simpleclaw/pull/546)을 squash merge(SHA `867c9c467aaf9d334a43186b685eb3fc588c6b76`)해 코드·오프라인 검증을 완료했다. 초기 [release PR #552](https://github.com/ingki3/simpleclaw/pull/552)는 `main` merge SHA `190135b0533f35b3b22e91e423ad00a13aa57ee3`과 tag `v2026.08.02`를 발행했으며, OpenRouter schema blocker를 BIZ-535/PR #553으로 해소한 뒤 [BIZ-532](mention://issue/08da18c2-4a3d-4c6b-802a-acc0f8160fe1)의 [release PR #555](https://github.com/ingki3/simpleclaw/pull/555) merge SHA `fb5490b8f3255e8a6b073f77dd10c8e9558531e7`, tag `v2026.08.02.1`로 final release와 `primary`/`legacy_v2` restart·health·no-send 3/3을 완료했다. 중복 [BIZ-530](mention://issue/9c325c7a-17ff-4f18-ae06-2025843e6bb2)과 PR #545는 미머지 취소됐고 최종 policy-compliant canonical 이력은 BIZ-531/PR #546이다. (완료: 2026-08-02)

- [x] **BIZ-519: RAG embedding을 Telegram polling 전에 pre-warm** — RAG 활성 runtime이 Admin API 준비 후 기존 `EmbeddingService`의 model-only pre-warm을 worker thread에서 await하고, 완료 뒤 Cron scheduler와 Telegram polling을 시작하도록 구현했다. PR #533을 `dev`에 squash merge(SHA `cd37dd1aa76270569c6d4cbfad8251dd009a5737`)하고 release PR #538을 `main`에 merge commit(SHA `15a6041dafe754e9aa591af46e2b7905ed2d611d`)으로 반영해 `v2026.08.01`을 발행했다. Live restart 후 prewarm success(`29381.01ms`)가 scheduler·polling보다 먼저 완료됐고, no-send reuse probe에서 384차원 vector·동일 model instance·두 번째 prewarm `0.01ms`를 확인했다. Admin·Telegram·Dashboard·Scheduler·Webhook health 정상, FD 19, 재시작 이후 ERROR 및 Telegram send/edit 0건을 확인했다. (2026-08-01)

- [x] **BIZ-508: 스포츠 상태 판정을 구조화 schema 기반으로 전환** — PR #522를 `dev`에 squash merge(SHA `c5e84cb97b37818545096dfeabdf880c013746ae`)하고 release PR #530을 merge commit(SHA `4fbbc377950b5ee04219f362a89f4b3e76a634d7`)으로 `main`에 반영해 `v2026.07.29`를 발행했다. Live `realtime-lookup-skill`은 timestamp backup 후 Naver Sports schedule schema·typed `lookup_status` 계약으로 갱신했고, LaunchAgent restart(`67645 → 72755`), Admin·Webhook·Telegram health, notifier 없는 no-send smoke(`found`, 롯데 3:5 한화, `live`, winner `null`)를 완료했다. 표시 문구·고정 키워드 없이 `STARTED → live`, `ENDED|RESULT → final`, 조회 실패와 명시적 경기 없음 분리를 운영에서 확인했다. (2026-07-29)

- [x] **BIZ-512: FunctionGemma 축소 intent·asset classifier QLoRA PoC** — BIZ-513/514의 candidate boundary·privacy·hard cap 보완은 canonical PR #527로 `dev`에 squash merge(SHA `b1c659b5821fe45368596e92a8d67464503e7fd6`)했고, BIZ-515 clean rerun 감사 결과는 PR #528로 squash merge(SHA `72b25d0c3b6d05d95632e221c98fff8fbd182048`)했다. superseded 구현 PR #525는 merge 없이 닫았다. 최초 44 label·augmentation·adapter 2개·report는 invalidated marker와 SHA-256으로 격리했다. clean rerun은 provider 300회에서 accepted 4건·raw identifier/out-of-set accepted 0을 확인했으나, 단일 QLoRA process가 return code 1로 종료되어 adapter를 생성하지 못했다. 따라서 base/tuned 비교는 N/A이며 shadow integration은 추천하지 않는다. live runtime 연결·config 변경·restart·deploy는 수행하지 않았다. (2026-07-29)

- [x] **BIZ-507: news-search-skill Gemini secret을 등록 skill 자식 범위로 제한** — PR #520을 `dev`에 squash merge(`62a203566fd2185e3b3dbec99824932bec3e0166`)했다. 2026-07-28 live rollout에서 `news-search-skill` N1~N3와 `krstock*` R1~R2 수정·timestamp backup, Gemini credential `file:gemini_api_key` encrypted-vault migration/read-back, LaunchAgent drain restart·Admin health, notifier 없는 registered skill 및 `/krstock-close` smoke를 완료했다.

- [x] **BIZ-503: 한국장 cron fresh-evidence 오판·성공 오기록 수정** — capability metadata 기반 evidence provider 판정, 중첩 error/stale/invalid fail-closed, 동일 record의 source/as-of/data 결합, 도메인별 fallback, structured semantic failure 영속화와 마지막 재시도 유형 기반 알림을 구현·검증했다. PR #514를 `dev`에 squash merge(SHA `e1d65e638afe586e7b2da136c8751d3ea8c1ef57`)하고 PR #516을 merge commit(SHA `b6658e5401b3ca3e162747bd0edb05f8ea3d9f99`)으로 `main`에 긴급 release하여 `v2026.07.27` 발행을 확인했다. live 스킬 timestamp backup/capability metadata/loader readback, release checkout 동기화, drain restart, Admin·Webhook·Telegram health, 실제 market-summary validator, notifier 없는 한국장 cron structured success smoke까지 완료했다. (2026-07-27)

- [x] **BIZ-497: Unified TurnPlanner canary 전환과 중복 라우팅 정리 — 코드·오프라인 검증 완료** — `off|shadow|canary|primary` 설정과 재시작에도 안정적인 user/chat cohort sampling을 추가하고, canary primary를 direct answer 및 선언된 read-only/no-side-effect 자산으로 제한했다. PlannerUnavailable·unknown asset·mutation confirmation은 semantic fallback 없이 fail-closed하며, primary/legacy/fail-closed 선택은 원문·식별자 없는 구조화 event로 계측한다. TurnAnalysis·Asset Selector·keyword router는 rollback window 한 release 동안 legacy 경로에만 유지하고 Phase 5 cleanup 시점을 문서화했다. 검증: canary/config/integration focused 14 passed, 관련 focused 77 passed, 전체 `tests/unit/` 2978 passed·3 xfailed, Unified pipeline integration 2 passed, fixed-gold 31 cases×3에서 schema/critical/pass 100%·context reduction 58.41%·failure 0, `ruff check src/ tests/ scripts/` 및 `git diff --check` clean, GitHub CI Unit Tests/Runtime Contracts/Offline Integration SUCCESS. **미완료 운영 단계:** 승인된 live benchmark, 100+ shadow sample, live canary와 7일 또는 운영자 승인 window, p50/p95·token·quality acceptance, 운영자 승인 후 config 반영·restart 및 static/follow-up/topic-shift/stock/weather/complex/recipe/mutation smoke. (2026-07-26)

- [x] **BIZ-496: ComplexFact·Recipe controller를 Unified plan 계약에 연결** — `FactCheckPlan.required_claims`를 재분류 없이 evidence slot으로 보존하고, selected standalone question·plan retrieval scope를 ComplexFactWorkflow에 전달한다. recipe mode는 primary recipe 하나만 노출하고 asset owner일 때 top-level fact controller를 실행하지 않는다. soft-404/canonical source/exact quote/unsupported number·date hardening이 아직 `dev`에 없어 `source_claim_hardening_ready=false` 기본 gate로 Unified complex production primary를 limited answer 처리한다. 검증: 신규/관련 focused 177 passed, 전체 `tests/unit/` 2969 passed·3 xfailed, Unified pipeline integration 1 passed, `ruff check src/ tests/ scripts/` 및 `git diff --check` clean. (2026-07-26)

- [x] **BIZ-495: Unified plan 선택 문맥·capability allowlist를 ToolLoop에 연결** — `primary` feature flag에서 Planner→PlanGate→ExecutionRouter를 연결하고 selected history·standalone question·exact skill/native tool scope를 한 turn 동안 고정했다. planned call은 ToolGate가 매 dispatch 전에 검증해 allowlist 밖 tool/skill을 structured observation으로 차단하며, safe direct asset는 primary 하나만 노출하고 current-fact plan은 evidence 전 streaming을 비활성화한다. production planner catalog에는 exact `execute_skill` 합성 어댑터를 포함하고, usable `web_search` 결과도 live evidence gate와 연결했다. `off/shadow`의 TurnAnalysis·Asset Selector·fuzzy skill legacy 경로는 유지했다. 검증: focused 196 passed, 전체 `tests/unit/` 2960 passed·3 xfailed, `ruff check src/ tests/ scripts/` 및 `git diff --check` clean. (2026-07-26)

- [x] **BIZ-494: Unified plan 기반 ExecutionRouter core 도입** — `execution.mode` 7종을 정확히 하나의 async callback으로 전달하고 결과를 변형 없이 반환하는 순수 `ExecutionRouter`/불변 `ExecutionCallbacks` 계약을 추가했다. Router는 planner·PlanGate·asset 선택·tool loop 구현을 소유하지 않으며, orchestrator에는 PlanGate PASS 이후 후속 Issue 8/9가 사용할 callback 생성 경계만 준비했다. default-off 기존 경로에서는 router가 생성·호출되지 않음을 회귀 테스트로 고정했다. 검증: focused planner/router/gate/orchestrator 78 passed, 전체 `tests/unit/` 2946 passed·3 xfailed, `ruff check src/` 및 `git diff --check` clean. (2026-07-26)

- [x] **BIZ-493: Unified TurnPlanner shadow 관측과 acceptance telemetry 연결** — default-off/sample 0의 background shadow planner, redacted aggregate event, fixed-gold 대비 acceptance 절차를 추가했다. Stage D 보강으로 structured row의 `trace_id=""`를 강제해 ordinary turn correlation을 차단하고 실제 JSONL 회귀 테스트를 추가했으며, strict planner compatibility 값인 `structured_output=true`·`repair_attempts=1`을 fail-closed 고정 계약으로 정규화했다. 검증: focused planner/config/gate/catalog/orchestrator 111 passed, 전체 `tests/unit/` 2937 passed·3 xfailed, Ruff 0.16 clean, `git diff --check` clean. (2026-07-26)

- [x] **BIZ-484: Google News RSS redirect를 publisher 원문으로 안전하게 해석** — BIZ-480 Stage D late finding hotfix. 실제 RSS의 `news.google.com/rss/articles/...` 후보를 RSS source publisher 홈페이지에서 제목 일치 same-site 원문 URL로 안전하게 해석하고, 기존 static redirect SSRF 검사 + `handle_web_fetch` block/headless 정책으로 원문을 fetch하도록 수정. Google article URL 성공/fail-closed, 외부·private link 거부, production orchestrator wiring 테스트를 추가. 검증: focused 137 passed, 전체 `tests/unit/` 2790 passed·3 xfailed, ruff clean, live no-send `AI 뉴스` smoke에서 publisher fact 1건/Google URL 0건, Graphify 12,222 nodes / 23,694 edges.

- [x] **BIZ-448: LLM routing policy — GLM default, Gemini fallback/multimodal** — 모델/프로바이더가 바뀌어도 운영자가 config 만으로 `default`/`fallback`/`multimodal` 역할을 지정할 수 있는 static 라우팅 정책 도입 (LangGraph 등 대규모 구조 변경 없음). (1) `config_sections/llm.py` — `llm.fallback`/`llm.multimodal` 키 로드(기본 None, 검증은 create_router 몫). (2) `llm/providers/openai_provider.py` — OpenAI-compatible endpoint 지원: `base_url`/`default_headers`/`extra_body` 생성자 파라미터 + `EXTRA_CONFIG_KEYS` 선언, send()/stream() Chat Completions 요청에 `extra_body` 주입(OpenRouter GLM-5.2 의 `reasoning.enabled=false` 강제 — 기본 reasoning 이 completion budget 을 잠식해 content_len=0 empty final 이 되는 문제 재현·해소 확인). (3) `llm/router.py` — static `provider:` alias(`openrouter_glm_5_2` → OpenAIProvider, config 전용·런타임 override 경로 없음), `fallback_backend`/`multimodal_backend` 필드+getter(미가용 이름은 경고 후 None 무력화), 암묵 요청 중 messages 에 attachments 있으면 multimodal 백엔드로 라우팅, 암묵 default 요청의 provider 예외/empty final(텍스트·tool_calls 모두 없음)은 fallback 백엔드로 1회 재시도. explicit `backend_name` 요청은 절대 자동 fallback/reroute 하지 않고, 스트리밍 경로는 혼합 출력 방지를 위해 fallback 미적용. (4) `scripts/dev/smoke_llm_routing_policy.py` — 임시 config 로 실제 router 경로 검증하는 no-secret 스모크(키는 존재/길이만 출력). (5) `config.yaml.example` — 주석 라우팅 정책 예시. 테스트: `test_llm_config_routing_policy.py`(3)/`test_openai_provider_openrouter.py`(5)/`test_router_provider_alias.py`(4)/`test_router_routing_policy.py`(12) 신설. 검증: `tests/unit/` 2651 passed·3 xfailed, ruff clean, 스모크 — text route GLM(z-ai/glm-5.2, 313자) / multimodal route Gemini 확인·키 값 미노출. Out of scope: live config.yaml default 전환(별도 운영 단계), OpenAI/OpenRouter required structured output, GLM multimodal 사용. (2026-07-17)

- [x] **BIZ-442: LaunchAgent restart drain/quiesce 절차 도입** — Hermes v0.18.0 gateway drain coordination 패턴 — runtime-affecting deploy 의 `launchctl kickstart -k` 재시작 전 새 intake 를 거절하고 in-flight turn 은 완료시키는 drain 창 도입. (1) 신설 `src/simpleclaw/daemon/drain.py` — `DrainState`/`DrainController`: drain 요청은 JSON state 파일(tmp+replace 원자적 쓰기)로 deploy script(별도 프로세스)와 bot 이 공유, deadline 경과 시 자동 만료(script crash 안전망), 깨진 파일 fail-open, active operation 카운터는 in-memory(`operation()` context manager). `request_drain(reason, timeout)`/`clear_drain()`/`is_draining()`/`active_operations()`/`status()`/`maintenance_notice()`. (2) `agent/orchestrator.py` — `process_message` 를 drain 게이트 wrapper 로 분리(`_process_message_impl` 위임): drain 중 새 intake 는 "점검 중" 즉답(대화 DB 미저장 — dreaming 오염 방지), `process_cron_message` 는 skip 사유 반환, 게이트는 진입 시 1회 평가라 진행 중 turn 은 안 끊김. config `daemon.drain.state_file`/`default_timeout` (기본 `~/.simpleclaw-agent/default/drain_state.json`/120s). (3) `channels/telegram_bot.py` `drain_notice_provider` — 인증 후 평가(비인가에 운영 상태 비노출), 콜백 예외 fail-open. (4) `channels/webhook_server.py` `drain_checker` — 인증 후 503 + Retry-After 30, `blocked_draining` metric. (5) `channels/admin_api.py`/`admin_routes/system.py` `drain_status_provider` — `/admin/v1/health` 에 `drain`(draining/reason/deadline/active_operations) 키. (6) 신설 `scripts/deploy/drain_restart_simpleclaw.py` — drain request → health 폴링 quiesce(active_operations=0, 폴링 불가 시 grace 대기 fallback) → timeout 결정(`--on-timeout proceed|abort`) → `launchctl kickstart -k gui/<uid>/<label>`(기본 `com.simpleclaw.bot`, env `SIMPLECLAW_LAUNCHAGENT_LABEL`) → health smoke → `--issue-id` 시 restart/health_smoke stage 를 VerificationEvidenceLedger 에 기록 → drain 해제(finally). subprocess/HTTP/clock 주입식이라 launchctl 없이 테스트 가능. 단위 테스트 40개 신규(controller 18, orchestrator 8, script 6, webhook 4, telegram 4)+admin health 3. 검증: `tests/unit/` 2557 passed, ruff clean, in-process e2e — 별도 컨트롤러 인스턴스 drain 요청 → 오케스트레이터 거절/복귀 확인. (2026-07-16)

- [x] **BIZ-441: Review/Deploy VerificationEvidenceLedger 도입** — Hermes v0.18.0 "done means proven, not claimed" — PR CI/release CI/deploy/restart/health smoke/product-intent/subagent gate 검증 결과가 코멘트로 흩어지던 것을 issue 단위 구조화 evidence 단일 source 로 강제. (1) 신설 `src/simpleclaw/review/verification_ledger.py` — `VerificationStatus`(passed/failed/pending/skipped)/`VerificationStage`(unit/lint/pr_ci/release_ci/deploy/restart/health_smoke/product_intent/subagent_gate, slug 형식 커스텀 stage 허용) enum, `VerificationEvidence` dataclass(issue_id/stage/status/pr_number/commit_sha/command/summary/raw_excerpt/source/created_at/updated_at), `VerificationEvidenceLedger` JSONL store(subagent_ledger 와 동일한 load→mutate→tmp+replace 원자적 저장, malformed line skip, now 주입). 같은 (issue_id, stage) 는 upsert(created_at 보존), `done_report()`/`done_allowed(required_stages)`/`missing_required_stages()` — required stage 는 passed evidence 필수(missing/failed/pending/skipped 전부 차단, fail-closed: 알 수 없는 status 복원은 pending), required 목록 밖 evidence 는 done 판정 미관여. redaction 은 저장 계층 강제 — raw_excerpt(2000자, 꼬리 보존)+command/summary(500자) 에 시크릿 패턴 마스킹(값 패턴 우선 적용해 `authorization: Bearer X` 의 X 잔존 버그 회피). retention 은 terminal record 만 정리(pending 은 기간 무관 보존). (2) `subagent_ledger.py` 에 read helper `gate_evidence(issue_id)` — required gate 미완료를 `subagent_gate` stage pending evidence 로 스냅샷(plain dict, 모듈 간 의존 없음). (3) operator native tool `verification_evidence`(scope=OPERATOR, operator_gate_required, risk=MEDIUM) — action=record/list/summary/done_allowed + `from_subagent_gate` 스냅샷. `tool_schemas.py`/`tool_dispatch.py`/`orchestrator._NATIVE_DISPATCH_TOOL_NAMES` 배선 + runtime scope dispatch 차단. (4) `config_sections/review.py` `verification_ledger` 섹션(기본 `~/.simpleclaw-agent/default/verification_evidence_ledger.jsonl`, retention_days 90) + `config.yaml.example` + `admin_policy.py` `review.verification_ledger.*` HOT. (5) 신설 `scripts/review/collect_verification_evidence.py` — pytest/ruff/gh checks/smoke 명령 실행 결과를 normalized evidence JSON 으로 출력(stage preset unit/lint, `--record` 로 ledger 직접 upsert, 실패 시 exit 1 로 CI gate 겸용). 테스트: `test_verification_ledger.py`(23)/`test_verification_evidence_tool.py`(17) 신설, operator registry 회귀 갱신(+1). 검증: `tests/unit/` 2515 passed·3 xfailed, ruff clean. Out of scope: Multica done transition hook, GitHub Actions workflow 대규모 변경, 자동 merge/deploy, Review Agent instruction 변경. (2026-07-16)
- [x] **BIZ-440: Subagent review result ledger 및 late-finding follow-up 처리** — "상위 agent 가 참고용 subagent review 를 띄움 → required gate 만 merge 차단 → optional late result 는 follow-up issue 로, 이미 merge/deploy 된 판정은 자동으로 뒤집지 않음" 운영 정책을 instruction 이 아닌 구조화 데이터로 강제. (1) 신설 `src/simpleclaw/review/subagent_ledger.py` — `ReviewGateKind`(required/optional) enum, `SubagentReviewRecord` dataclass(id/issue_id/parent_run_id/spawned_by/purpose/gate_kind/status/started_at/deadline_at/completed_at/merge_blocking/result_summary/finding_severity/followup_issue_id/followup_note/source_metadata), `SubagentReviewLedger` JSONL store(load→mutate→tmp+replace 원자적 저장, malformed line skip, now 콜백 주입). merge_blocking 은 생성 시 gate_kind 에서 한 번만 파생(감사 근거 보존), `can_merge(issue_id)` 는 required 미완료 record 가 하나라도 있으면 False, `late_records()` 는 deadline 경과+미완료(명시 late 포함), `mark_late`/`link_followup` 은 해당 record 필드만 갱신 — parent 상태를 만지는 API 자체를 두지 않음. retention 은 완료 record 만 정리(미완료 required 는 기간 무관 보존). (2) operator native tool `review_subagent_ledger`(scope=OPERATOR, operator_gate_required, risk=MEDIUM) — action=start/complete/mark_late/create_followup_note/list. `tool_schemas.py`/`tool_dispatch.py`/`orchestrator._NATIVE_DISPATCH_TOOL_NAMES` 배선 + runtime scope dispatch 차단. mark_late/create_followup_note 응답에 `parent_state_changed: false` + follow-up 안내 계약(실제 issue 생성 자동화는 v1 범위 밖 — draft/note 연결까지만). (3) `config_sections/review.py` `load_review_config()`(path 기본 `~/.simpleclaw-agent/default/review_subagent_ledger.jsonl`, retention_days 90) + `config.py` facade export + `config.yaml.example` review 섹션 + `admin_policy.py` `review.subagent_ledger.*` HOT(호출 시점 config 재해석). 테스트: `test_review_subagent_ledger.py`(18)/`test_review_subagent_ledger_tool.py`(19) 신설, operator registry 회귀 갱신(+1). 검증: `tests/unit/` 2474 passed·3 xfailed, ruff clean. Out of scope: 실제 subagent spawn, merged PR/deploy 상태 자동 변경, follow-up issue 생성 자동화, VerificationEvidenceLedger 연동(후속). (2026-07-16)
- [x] **BIZ-435: Recipe learning risk_flags 및 structured-output logging hardening** — BIZ-432 의 skill learning hardening 을 RecipeSuggestion 경로에 동일 원칙으로 적용. (1) `recipes/learning.py` — `RECIPE_RISK_FLAG_ALLOWLIST`(skill 공용 allowlist + recipe 전용 `cron_hint`) + `normalize_recipe_risk_flags()`(skill 쪽 `normalize_risk_flags()` 에 `allowlist=` 파라미터를 추가해 재사용, drop 된 값은 secret 가능성 때문에 로그 미기재·개수만 warning) 신설, `RecipeSuggestion.new_pending()`/`from_dict()`(legacy 저장분 정화)/`suggestion_from_recipe_payload()` 모든 생성 경로에 적용. (2) trace 기반 위험 감지 — `detect_trace_risk_flags()` 신설: tool 이름 토큰(web/fetch/cli/shell/write/manage...)과 redacted arguments 텍스트(http(s)://, api_key/authorization, `_RISK_KEYWORDS`)에서 network/subprocess/file_write/external_api 를 감지해 LLM payload 가 빈 risk_flags 를 줘도 합산. (3) schema strictness — `RECIPE_SUGGESTION_RESPONSE_SCHEMA` root 와 parameter item 에 `additionalProperties: false`, `risk_flags.items` 에 allowlist enum(cron_hint 는 내부 파생값이라 제외). (4) `orchestrator.py` — `_draft_recipe_suggestion()` 에 skill 과 동일한 안전 catch: 실패 시 raw 응답/프롬프트/exception 메시지 대신 exception class·structured flag·raw_len 만 warning 로그 후 `None` 반환, `_capture_recipe_learning_candidate()` 는 `None` 이면 저장 skip. (5) `recipe_learning_tool.py`/`config_sections/agents.py`/`config.yaml.example` — recipe materialize 는 어떤 config 로도 pending 후보를 설치할 수 없고 항상 accepted + confirm=true 이중 게이트를 유지, `require_operator_accept` dead key 는 loader/example 에서 제거. 회귀 테스트: trace 기반 감지(file_write/cli/web_fetch → allowlisted flags)·payload/legacy 정화·schema additionalProperties/enum(`test_recipe_learning.py` +9), provider 예외/invalid JSON 에서 raw 미로그 + 후보 미저장·non-object JSON skip(`test_orchestrator_recipe_learning.py` +3), legacy 저장분 list/show 정화 + trace 요약 유지 및 `require_operator_accept=False` 에서 pending materialize 거부(`test_recipe_learning_tool.py` +2). 검증: focused 50 passed, `tests/unit/` 2407 passed·3 xfailed, ruff clean. Out of scope: BIZ-428 recipe learning 재설계, live `recipes.dir` 자동 materialize/cron 자동 생성, SkillSuggestion 동작 회귀, `file_write` sandbox 확대. (2026-07-12)
- [x] **BIZ-432: BIZ-429 follow-up — materialize 승인 게이트와 risk_flags 안전화** — Stage D 병렬 리뷰에서 확인된 blocker 2건 보정. (1) accepted 게이트 우회 제거 — `skills/materializer.py` 의 `require_accepted` opt-out 파라미터 삭제(accepted 아니면 무조건 PermissionError), `agent/skill_learning_tool.py` 의 `require_operator_accept` config 분기 삭제(materialize 는 어떤 config 로도 accepted+confirm 이중 게이트 유지), `config_sections/agents.py` 에서 dead key `require_operator_accept` 제거(config.yaml 에 남아 있어도 무시). (2) `risk_flags` 안전화 — `skills/learning.py` 에 `RISK_FLAG_ALLOWLIST`(network/subprocess/file_write/external_api) + `normalize_risk_flags()`(대소문자/`-`→`_` 정규화 후 allowlist 밖 값은 전부 drop, 버린 값은 secret 가능성 때문에 로그에도 미기재·개수만 warning) 신설, `SkillSuggestion.new_pending()`/`from_dict()`/`suggestion_from_candidate_payload()` 모든 생성 경로에 적용(orchestrator pending 로그도 자동으로 정화된 값만 노출), structured output schema 의 `risk_flags.items` 에 allowlist enum 제약 추가. 회귀 테스트: secret-like/unknown risk_flags drop·allowlist 보존·legacy 저장분 정화·schema enum(`test_skill_learning.py` +7), `require_operator_accept=False` 에서도 pending materialize 거부(`test_skill_learning_tool.py` +1), opt-out kwarg 제거 TypeError + pending/rejected/materialized 거부 parametrize(`test_skill_materializer.py`). 검증: 실패 재현 후 수정, focused 52 passed, `tests/unit/` 2308 passed·3 xfailed, ruff clean. Out of scope: BIZ-429 의 schema/prompt/show·diff UX 재작성, RecipeSuggestion(BIZ-428), live deploy/restart. (2026-07-12)
- [x] **BIZ-426: LLM 기반 TurnAnalysis 라우팅 도입** — 운영자가 원치 않는 keyword/길이 heuristic 기반 follow-up·route·capability 판단을 LLM structured JSON 판단으로 전환. (1) 신설 `agent/turn_analysis.py` — `TurnAnalysis` dataclass(원문/정규화 질문/is_followup/confidence/needs_clarification/ambiguity_options/domains/intents/route/complexity 슬롯 플래그/source) + `to_route_decision()`, `parse_turn_analysis_payload()`(JSON fence 제거, route/confidence/complexity clamp, 리스트 sanitize, 빈 정규화 질문 원문 복귀, 저신뢰(<0.65) clarify 승격), `analyze_turn_with_llm()`(최근 대화 직렬화 + LLMRouter 호출, 실패 시 원문 보존 `source=fallback` 보수 결과). (2) 신설 `prompts/system/turn_analysis.yaml` — JSON only/user answer 금지/원의미 보존/ambiguity 시 2-4 clarify options/route enum 선택 계약. (3) `orchestrator.py` — ordinary turn 에서 `build_turn_frame()` 대신 LLM 분석이 primary. `route_input=turn_analysis.normalized_question`, ambiguity 는 기존 ClarifyRequest UX, route 는 `to_route_decision()`, `select_capability()` 에 LLM intents/domains explicit 전달. 분석 비활성/실패 시에만 기존 결정적 TurnFrame+classify_response_route fallback(운영 연속성). (4) `capability_router.py` — `select_capability(explicit_intents=, explicit_domains=)` 확장, keyword 추론은 fallback, read-only+무부수효과 안전장치/weak-intent/도메인 오매칭 가드 유지. (5) `config_sections/agents.py` + `config.yaml.example` — `agent.turn_analysis`(enabled 기본 true/backend/max_tokens/max_recent_messages/fallback_mode). (6) `response_router.py`/`turn_frame.py` docstring 을 deprecated fallback 으로 재정렬. 테스트: `test_turn_analysis.py`(15)/`test_orchestrator_turn_analysis.py`(9) 신설, capability router(+4), 결정적 경로 검증 fixture 들은 `turn_analysis.enabled=false` 로 고정. 검증: `tests/unit/` 2240 passed·3 xfailed, ruff clean. Out of scope: LangGraph 도입, ToolLoopRunner/ComplexFactWorkflow 재작성, keyword fallback 파일 삭제(후속 cleanup), live deploy/restart(운영 단계). (2026-07-07)
- [x] **BIZ-425: Context-normalized capability routing 도입** — follow-up/축약 질문이 원문만으로 complex 과승격되거나 스킬 선택이 빗나가는 문제를, 도메인별 Python override 없이 "질문 정규화 + capability metadata 기반 read-only 자산 우선" 원칙으로 해결. (1) 신설 `agent/turn_frame.py` — `TurnFrame`(original_text/normalized_question/context_summary/confidence/ambiguity_options)·`build_turn_frame()`: 최근 대화를 user 발화 단위 `ContextCandidate` 로 묶고(키워드 겹침 병합, 조사/어미 근사 스트리핑, 도메인 분기 없음) follow-up cue 검출 시 맥락 키워드 접두 정규화. 복수 후보 + 지시대명사형이면 confidence 0.45 + options 로 clarify 유도, 원문은 절대 미변경. (2) 신설 `simpleclaw/capability.py` — 공용 `CapabilityMetadata`(domains/intents/read_only/side_effects/freshness_sensitive/direct_answer/requires_confirmation/declared) + `parse_capability_metadata()`; 미선언/오류는 보수 기본값(read_only=False, side_effects=True)으로 자동 실행 후보 제외. `skills/discovery.py`(SKILL.md frontmatter)·`recipes/loader.py`(recipe.yaml) 파싱 배선. (3) 신설 `agent/capability_router.py` — 일반 의도 cue(standings/current_result/schedule/quote/weather/news/daily_report/realtime_lookup)와 도메인 힌트를 자산 선언 metadata 와 교집합 매칭해 `CapabilityDecision` 산출. 명시 선언 read-only+무부수효과만 후보, 도메인 오매칭 필터, 시점 cue 단독 매칭 금지. (4) `orchestrator.py` — `process_message()` 초반 TurnFrame 생성, route/classifier/study context 는 `normalized_question` 사용, DB 저장은 원문 유지, ambiguity ≥2 면 기존 clarify 인프라(BIZ-260)로 되묻기, read-only capability 는 complex 과승격보다 먼저 고려(단 `needs_remaining_variables` 질문은 complex 유지), capability hint 시 selector LLM 우회 + 자산 후보 축소(Option B — direct 실행 대신 강한 힌트), TurnFrame/capability/route decision structured log(원문 미노출). (5) `progress.py` — complex_fact 슬롯 5종 한국어 라벨 맵 + 내부 영어 slot question 사용자 미노출. 테스트: `test_turn_frame.py`(8)/`test_capability_metadata.py`(8)/`test_capability_router.py`(8)/`test_progress_formatting.py`(5) 신설, orchestrator routing(+6)/response_router(+3) 보강. 검증: `tests/unit/` 2212 passed·3 xfailed, ruff clean. Out of scope: 실제 runtime skill/recipe metadata 보강(후속), DB schema 변경, LLM context resolver, direct skill 실행. (2026-07-05)
- [x] **BIZ-424: SimpleClaw MCP runtime 지원 구현** — config.yaml `mcp.servers` 기반 stdio MCP 서버 연결/도구 노출/호출을 실제 runtime 경로에 배선. (1) `config_sections/mcp.py` — `load_mcp_config()`: enabled/servers 검증·정규화(stdio only, invalid shape drop, timeout/connect_timeout 양수 폴백, scope 기본 operator). (2) `MCPManager` 하드닝 — `(server, tool)` 충돌-안전 레지스트리, tool `metadata`(input_schema/scope) 보존, 서버별 `get_connection_errors()`, subprocess env filtering(baseline+명시 env만, secret resolver hook), scope 검사 포함 `call_tool(server, tool, args, scope=...)`, connect/call timeout. (3) orchestrator — `_ensure_mcp_connected()` lazy one-shot 연결, runtime-scope tool 존재 시에만 generic native tool `mcp_call` 노출(operator context 완화), `ToolDefinition.metadata` 필드 추가. (4) `tool_dispatch` — `mcp_call` 라우팅 + context 파생 scope(LLM 인자로 조작 불가). (5) `asset_inventory(type=mcp)` — configured/connected/tools(scope/has_input_schema)/connection_errors 요약, env는 키만 노출. (6) `tool_usage` prompt v2 — inventory 우선 확인·미설정 MCP tool 추측 금지 지침. 테스트: `test_mcp_config.py`(5)/`test_mcp_client.py`(+8)/`test_tool_dispatch_mcp.py`(6)/`test_asset_inventory_mcp.py`(3)/`test_orchestrator_mcp.py`(5)/tool_schemas(+4) + real-SDK fake stdio server 통합 smoke 2개. 검증: `tests/unit/` 2170 passed·3 xfailed(pre-existing `test_equity_research_recipes_discoverable` 실패는 live recipe allowlist drift — 본 이슈 무관, follow-up 분리), ruff clean. Out of scope: HTTP transport, resources/prompts, per-tool 동적 스키마 주입, hot-reload. (2026-07-05)
- [x] **BIZ-408: BIZ-406 후속 — pinned topic record demotion 회귀 수정** — 운영자가 `pin()` 한 topic 이 이후 user/Dreaming/news 신호가 들어오면 `EvolvingTopicRegistry.record()` → `_refresh()` → `_next_state()` 경로에서 ACTIVE/CANDIDATE/COOLING 등으로 자동 demote 되던 correctness 회귀를 수정. `evolve()` 만 pinned 예외가 있고 `record()` 에는 sticky 예외가 없던 것이 원인. (1) `_refresh()` 를 점수만 갱신하는 `_refresh_score()`(interest_score/peak_score/updated_at)와 상태 전이를 붙인 `_refresh()` 로 분리. (2) `record()` 가 기존 topic 이 `PINNED` 면 `_refresh_score()` 만 호출해 점수/메타/last_signal_at 은 갱신하되 상태는 PINNED 유지(`unpin()` 전까지). (3) `evolve()` pinned 분기도 동일 헬퍼 재사용으로 통일. 회귀 테스트 2종 추가(`test_pinned_topic_remains_pinned_when_new_signal_recorded`, `test_pinned_topic_updates_score_without_demoting`). 검증: `tests/unit/` 2039 passed·3 xfailed, ruff clean. Out of scope: scoring 가중치/임계값, naive/aware datetime 방어, to_records↔load_topics 포맷은 별도 follow-up. (2026-07-01)
- [x] **BIZ-390 / BIZ-406: Agent Study Wiki — 진화형 topic registry와 scoring/decay (최신 dev 재통합)** — 사용자의 관심사가 고정되지 않도록 candidate/active/pinned/cooling/archived 생애주기와 scoring/decay 를 구현하고, 선행 PR #401 이 최신 dev 의 `topics.yaml` persistence registry 와 add/add 충돌하던 것을 두 기능 모두 보존하도록 재통합. (1) 신설 `src/simpleclaw/study/scorer.py` — user_interest(0.35)/repeated_mentions(0.20)/freshness_need(0.20)/global_importance(0.15)/recency_decay(0.10) 가중합 + clamp `compute_topic_score`, `ScoreWeights`(합=1.0 강제), 포화 곡선 `normalize_mentions`, 지수감쇠 `recency_decay_factor`. (2) `topic_registry.py` 통합 — 기존 dev persistence(`TopicRegistry`/`load_topics`/`save_topics`, `topics.yaml`↔`StudyTopic`)는 그대로 두고, 진화형 레지스트리는 이름 충돌을 피해 `EvolvingTopicRegistry`(record/ingest_dreaming_signals/pin/unpin/evolve/study_targets/to_records), 점수 신호는 `TopicSignal`(interest_signals.InterestSignal 과 구분), `Topic`/`TopicState`/`SignalSource`/`TopicEvolutionPolicy` 로 분리 추가. 시간 주입(now_fn)으로 감쇠/아카이브 결정적 재현, peak_score 승격 주제는 감쇠창 2배, pinned sticky. (3) `__init__.py` — dev exports 전부 보존 + scorer/evolution 심볼 추가 export. (4) 테스트 — `test_study_topic_registry.py` 에 dev persistence 테스트 + lifecycle 테스트 병합, `test_study_scorer.py` 신설. 검증: `tests/unit/` 2037 passed·3 xfailed, ruff clean. DoD: lifecycle 지원·Dreaming signal 기반 생성·일회성 cooling/archive·dev persistence 회귀 없음. (2026-07-01)
- [x] **BIZ-394: Agent Study Wiki — Freshness/confidence gate + complex routing 보강** — 현재성/영향/시나리오 질문이 stale 한 wiki 배경지식만으로 답해지는 문제를 구조적 cue + freshness gate 로 봉합. (1) `response_router.py`: 특정 사건 키워드 대신 "도메인 이벤트(상장/IPO/공모/기업가치/시총/실적/가이던스/증시 등) + 분석/영향(영향/파급/전망/리스크/조사/분석/원인/배경 등)" 조합을 `needs_impact_analysis` 로 잡아 현재 사실 근거를 강제(분석 cue 단독은 비가중). `assess_study_context()` + `study_context` 인자: 주입될 study 블록이 stale/저신뢰면 현재성·영향·시나리오 질문에 한해 at-least guarded 로 escalate(`study_context_unreliable`). (2) `context_retrieval.py`: `annotate_study_freshness()` — study 블록의 `Updated:`(기본 14일)·`Confidence:`(기본 0.5) 메타를 시계와 비교해 stale/저신뢰면 `Freshness: stale` 마커 + 한계 명시/재조회 강제 지시문을 주입(메타 없으면 무판정). 서비스에 `now_provider` 주입, `study_context_for_routing()` 추가. (3) `orchestrator.py`: 라우팅 전에 study 신선도 신호를 라우터로 전달, `_LIVE_FACT_STOCK_TERMS` 에 기업 이벤트(상장/ipo/공모/기업가치/시총) 추가 → "OpenAI 상장 영향" 류가 시간 cue 없이도 evidence 조회. (4) `realtime_lookup.py`: `classify_query` market cue 를 모듈 상수로 추출·구조적 확장. 회귀 테스트: `test_response_router_study_context.py`(14, 신설) + realtime/orchestrator/context 회귀 보강. 검증: `tests/unit/` 1981 passed·3 xfailed, ruff clean. DoD: OpenAI IPO/증시 영향→비-standard, 월드컵 경우의 수→complex, stale/저신뢰 study→한계/재조회 강제, 구조적 cue+regression. (2026-06-30)
- [>] **BIZ-393: Agent Study Wiki — retrieval 을 대화 context 에 연결** — 매일 공부해 쌓은 Study Wiki 배경지식을 질문 시 회수해 system prompt 에 주입한다. 신설 `src/simpleclaw/study/retriever.py` `StudyRetriever`: wiki on-disk 레이아웃(`topics.yaml` + `topics/<id>.md`)을 읽어 질문 토큰과의 lexical overlap(label/tags 가중 + page 불릿)으로 topic 을 점수화, pinned>active>archived 우선·동점 안정정렬로 결정적 랭킹, `## Agent Study Context` 블록(사용자 메모리 아님 + 현재성 재확인 경고 EN/KO, Topic/Updated/Confidence/Relevant notes/Sources)을 `max_context_chars` 예산 내로 결정적 절단. archived 는 일반 질문 제외·`historical=True` 일 때만 회수. `ContextRetrievalService` 는 study 회수를 대화 RAG/임베딩과 독립 경로로 합치고(`_retrieve_study_context`) 예외를 이중 격리. orchestrator 는 `load_study_config` 의 `study.enabled && retrieval.enabled` 일 때만 활성. 단위 테스트 18개(`test_study_retriever.py` 12, `test_context_retrieval_agent_study.py` 6). 검증: `tests/unit/` 1939 passed·3 xfailed(BIZ-394 e2e 계약 대기), ruff clean. (진행: 2026-06-30)
- [x] **BIZ-387: Agent Study Wiki — Study Wiki 파일/Markdown 저장소 MVP** — 사람이 직접 읽고 고칠 수 있는 Markdown 을 source of truth 로 삼는 위키 저장소 계층을 신설. `src/simpleclaw/study/` 패키지 4종: `types.py`(StudyItemStatus str-enum, frozen StudySource, StudyTopic, StudyPage dataclass), `paths.py`(런타임 루트 `~/.simpleclaw-agent/default/study/` 아래 `topics.yaml`/`topics/`/`daily/` 경로 규약 + 멱등 `init_wiki_root` + topic_id 경로 인젝션 방지), `markdown.py`(StudyPage ↔ Markdown 왕복 직렬화 — 최소 frontmatter[topic_id/title/updated_at] + 한국어 섹션 5종 + 출처 불릿, 손편집 관대 파싱), `topic_registry.py`(topics.yaml ↔ StudyTopic 관대 로드/정규 원자적 저장 + TopicRegistry CRUD). 단위 테스트 15개(`test_study_types.py`/`test_study_markdown.py`/`test_study_topic_registry.py`) 모두 통과, ruff clean. (2026-06-29)
- [x] **BIZ-389: Agent Study Wiki — Dreaming/대화 기반 관심사 signal 추출** — 공부 대상이 static list 가 아니라 사용자가 물었거나 관심을 보인 주제에서 진화하도록, 기존 신호(Dreaming 산출물·대화 클러스터·승격 insight·최근 사용자 질문)를 `InterestSignal`(topic_hint/text/source/source_ref/weight/confidence/last_seen)로 정규화하는 read-only 모듈 `src/simpleclaw/study/interest_signals.py` 추가. (1) `extract_topic_hints(user_messages/memory_items/insights/auto_reports)` 오케스트레이터 + source 별 빌더(`signals_from_user_messages/memory_items/insights/auto_reports`). (2) **과대 일반화 방어** — cron/recipe 자동 산출물(`auto_report`)은 항상 `AUTO_REPORT_MAX_WEIGHT`(0.3) 미만으로 클램프해 organic 사용자 질문(base 0.6)보다 절대 높지 않게 함. (3) memory_items 는 계획이 지정한 4종(accepted_user_insight/active_project/decision/cluster_summary)만 채택, insights 는 promoted 또는 confidence≥0.6 만. (4) 반복 질문은 키워드 포함도(≥0.6) greedy 그룹핑으로 묶어 weight/confidence 가산. dict·dataclass(Enum type 포함) duck typing. 결과는 weight 내림차순 정렬. 단위 테스트 22개(`test_study_interest_signals.py`). 검증: `tests/unit/` 1920 passed, ruff clean. 사용자 메모리 파일 미수정(읽기 전용). (2026-06-30)
- [x] **BIZ-395: Agent Study Wiki 운영자 조회/강제 업데이트/관찰성** — Agent Study Wiki(매일 공부해 쌓는 외부 세계 배경지식)를 운영자만 점검/조작할 수 있는 관찰성 계층을 추가. 수집 파이프라인(`simpleclaw.study`, 선행 이슈)이 아직 없어도 graceful 하게 동작하도록, on-disk 레이아웃(`topics.yaml` + `daily/*.md` + `index.sqlite`)을 직접 읽는 self-contained `StudyWikiStore`(`src/simpleclaw/agent/study_status.py`)를 두고 wiki 미구성 시 `configured=false`로 응답한다. (1) operator native tool `study_status`(scope=OPERATOR, operator_gate_required, risk=MEDIUM) — action=status(최근 run·active/stale topic·low-confidence item), topics, show, refresh(다음 daily run 재수집 플래그만 기록), archive. `tool_schemas.py`/`tool_dispatch.py`/`orchestrator._NATIVE_DISPATCH_TOOL_NAMES` 배선 + operator gate 차단. (2) Admin API route group `admin_routes/study.py`(`GET /admin/v1/study/{status,topics,topics/{id}}`, `POST .../{refresh,archive}`) — service 미주입 시 config.yaml `study.wiki_dir`로 지연 구성. low-confidence는 `index.sqlite study_items` 우선, 없으면 topic confidence 폴백. 단위 테스트 26개(`test_study_status_tool.py` 18, `test_admin_api_study.py` 8) + operator registry 회귀 갱신. 검증: `tests/unit/` 1874 passed, ruff clean. 일반 사용자 runtime tool 비노출. (2026-06-29)
- [x] **BIZ-383(검색 품질): realtime-lookup 멀티소스 본문 추출 + web_search 본문 보강** — 일정/상태성 답변에서 숫자가 틀리던 근본 원인(① web_search가 DuckDuckGo snippet만 반환, ② realtime-lookup이 네이버 SERP chrome을 1800자로 잘라 사용, ③ 초경량 모델이 빈약한 근거로 수치 환각)을 검색 품질 측면에서 봉합. (1) `realtime_lookup`: 결과 링크 발견을 DuckDuckGo HTML 엔드포인트로 전환(`discover_result_links`/`extract_result_links`+uddg 디코드), 상위 N개(기본 2) 실제 기사 본문을 `_html_to_text`(nav/header/footer chrome 제거)로 추출해 멀티소스 evidence 생성, 본문 회수 실패 시 네이버 SERP 텍스트 폴백, confidence high/medium/low + 출처별 timeline_status. (2) `builtin_tools.handle_web_search`: `body_fetcher` 주입 시 상위 결과 본문 발췌(`_fetch_search_result_body`, 차단/오류 graceful skip) 동봉, dispatch 배선. 단위 테스트 확장(`test_realtime_lookup_skill.py`, `test_web_search_tool.py`), 라이브 스모크 확인(실 기사 본문 회수). 전체 `tests/unit/` 1794 passed, ruff clean. 후속: live-fact 게이트(`_looks_like_live_fact_request`) 키워드 확장은 별도. (완료: 2026-06-27)
- [x] **BIZ-378: 운영자 승인형 restart_runtime native 도구 추가** — operator gate에서만 노출되는 `restart_runtime` native tool을 추가해 `confirm=true` + `reason` 명시 시에만 macOS LaunchAgent `kickstart -k`를 수행하고, 재시작 후 live cwd, PID 변경, Admin API health, Telegram/scheduler/dashboard flags, FD count를 JSON으로 반환하게 했다. 실패 시 `log_debug` 후속 진단 힌트를 포함하며, 일반 runtime context에서는 비노출/수동 dispatch 차단된다. 단위 테스트 6개(`tests/unit/test_restart_runtime_tool.py`)와 registry/operator dispatch gate 회귀 업데이트 포함. 검증: `tests/unit/` 1748 passed, `uv run --extra dev --with ruff python -m ruff check src/` 통과. live config/recipe/skill 파일 수정 없음. (2026-06-14)
- [x] **BIZ-377: 운영자용 skill_validate native 도구 추가** — operator/development gate에서만 노출되는 `skill_validate` native tool을 추가해 configured skills local/global dir 기준 runtime skill discovery, SKILL.md metadata, `script_path` 추론, script/venv python runner 존재 여부를 read-only JSON으로 검증할 수 있게 했다. 기본은 smoke 실행 없이 읽기 전용이며, `smoke=True`일 때만 짧은 `--help` 실행을 timeout/redaction과 함께 수행한다. 단위 테스트 8개(`tests/unit/test_skill_validate_tool.py`)와 registry/operator dispatch gate 회귀 업데이트 포함. 검증: `tests/unit/` 1742 passed, `uv run --extra dev --with ruff python -m ruff check src/` 통과. (2026-06-14)
- [x] **BIZ-375: 운영자용 deploy_status native 도구 추가** — operator scope에만 노출되는 `deploy_status` native tool을 추가해 live checkout의 branch/HEAD, origin/main 또는 origin/dev ahead/behind, dirty paths와 deploy range overlap, origin/main..origin/dev unreleased commit, open PR summary를 read-only JSON으로 확인할 수 있게 했다. `compare=main|dev`, `include_prs` 옵션을 지원하고, `gh` 실패 시 git-only summary로 graceful fallback한다. 단위 테스트 5개(`tests/unit/test_deploy_status_tool.py`)와 registry/operator gate 회귀 업데이트 포함. 검증: `tests/unit/` 1727 passed, `uv run --extra dev --with ruff python -m ruff check src/ tests/unit/test_deploy_status_tool.py tests/unit/test_operator_tool_registry.py` 통과. (2026-06-14)

- [x] **BIZ-376: 운영자용 recipe_validate native 도구 추가** — operator/development gate에서만 노출되는 `recipe_validate` native tool을 추가해 configured `recipes.dir` 기준 recipe `name`/`path` resolve, YAML parse/필수 필드 오류 JSON화, empty/provided `render_params` smoke, `/cron`·`/undo` slash collision warning을 read-only로 확인할 수 있게 했다. 단위 테스트 7개(`tests/unit/test_recipe_validate_tool.py`)와 registry gate 회귀 업데이트 포함. 검증: `tests/unit/` 1729 passed, `uv run --extra dev --with ruff python -m ruff check src/` 통과. live recipe 파일 수정 없음. (2026-06-14)

- [x] **BIZ-374: 운영자용 asset_inventory native 도구 추가** — operator scope에만 노출되는 `asset_inventory` native tool을 추가해 native tool registry(scope/risk/enabled), SimpleClaw runtime skill(source dir/script_path/executable), recipe path/parse status, MCP server/tool 요약, selector config를 read-only JSON으로 확인할 수 있게 했다. Hermes skill과 runtime skill 혼동 방지를 위해 skill 항목에 `source=simpleclaw_runtime_skill`을 명시한다. 단위 테스트 7개(`tests/unit/test_asset_inventory_tool.py`)와 registry/operator gate 회귀 업데이트 포함. 검증: `tests/unit/` 1722 passed, `uv run --extra dev --with ruff python -m ruff check src/` 통과. (2026-06-14)

- [x] **BIZ-373: 운영자용 log_debug native 도구 추가** — operator scope에만 노출되는 `log_debug` native tool을 추가해 `/Users/simplist/.simpleclaw-agent/default/bot.log` 기준 최근 로그, ERROR/Traceback, trace_id, tool_loop/recipe/skill/telegram/admin_api/scheduler 관련 줄을 read-only JSON으로 확인할 수 있게 했다. 토큰/API 키/긴 사용자 본문 redaction, line/pattern limit, 파일 읽기 실패 LLM-readable 오류를 포함한다. 단위 테스트 6개(`tests/unit/test_log_debug_tool.py`)와 registry/operator dispatch gate 회귀 업데이트 포함. 검증: `tests/unit/` 1715 passed, `.venv/bin/python -m ruff check src/` 통과. (2026-06-14)

- [x] **BIZ-372: 운영자용 config_inspect native 도구 추가** — operator scope에만 노출되는 `config_inspect` native tool을 추가해 `/Users/simplist/.simpleclaw/config.yaml` 기준 effective config 요약을 read-only JSON으로 확인할 수 있게 했다. `section=all|llm|agent|memory|skills|recipes|daemon|admin_api|security`, `resolve_paths`, `redact` 옵션을 지원하고, 실제 token/api_key/master_key 값은 마스킹하되 `file:admin_api_token` 같은 시크릿 참조는 보존한다. 단위 테스트 5개(`tests/unit/test_config_inspect_tool.py`)와 registry/operator gate 회귀 업데이트 포함. 검증: `tests/unit/` 1709 passed, `uv run --extra dev --with ruff python -m ruff check src/` 통과. (2026-06-14)

- [x] **BIZ-370: 운영자 native tool scope registry 추가** — native function calling tool registry에 scope/risk/operator gate metadata를 추가하고, 기본 runtime context에서 operator/development 도구가 노출되지 않도록 회귀 테스트를 보강했다. `tests/unit/test_operator_tool_registry.py` 추가, `test_tool_schemas.py` scope filtering 보강, 전체 `tests/unit/` 1529 green + `ruff check src/` 통과. (2026-06-14)

- [x] **BIZ-243: Recipe loader 미지원 키 무성 폴백 + v1 PROMPT step silent no-op 봉합** — 2026-05-18 cron-krstock-auto 사고(`tool:`/`prompt:`/`args:` 등 비슷한 이름의 미지원 키가 무성 폴백되어 빈 content PROMPT step 2개로 파싱, scheduler 가 빈 prompt_output 으로 LLM 호출을 스킵, "Recipe completed: 2/2" 빈 통지가 텔레그램 발송) 봉합. (1) `loader.py` `_VALID_STEP_KEYS = {type, name, content, on_error, rollback}` 화이트리스트 도입, 미지원 키와 type=prompt+빈/공백 content 를 `RecipeParseError` 로 즉시 실패. (2) `scheduler.py._execute_action` v1 분기에 안전망 — recipe.steps 에 PROMPT 가 있는데 prompt_output 이 비면 WARN 로그(silent no-op 재발 시 즉시 운영자 인지). (3) `executor.py` 모듈 docstring 갱신: 호출자 책임 약속을 BIZ-243 사고 맥락과 함께 명시. 단위 테스트 11개 추가(`test_recipe_loader.TestStrictStepValidation` 7개 — 미지원 키/prompt-alias/빈·공백·누락 content/COMMAND 빈 허용/유효 키 통과; `test_scheduler.TestRecipeCronInvokesLLM` 4개 — v1 PROMPT step LLM 호출/COMMAND+PROMPT 합쳐 LLM 호출/COMMAND-only 폴백/합성 빈 PROMPT WARN 안전망). `docs/feature-recipes.md` 에 v1 스텝 허용 키와 호출자 책임 섹션 추가. 전체 `tests/unit/` 1168 green (2026-05-18)
- [x] **BIZ-202: 레시피 디렉터리를 `~/.simpleclaw/recipes/` 로 이전 — 봇/데몬 절대 경로 통일** — 봇이 채팅에서 작성한 레시피가 `~/.simpleclaw/workspace/.agent/recipes/` 에 떨어지고 데몬은 working tree CWD 기준 `.agent/recipes/` 를 봐서 등록 누락되던 사고(2026-05-15 krstock) 봉합. `config.yaml` 에 `recipes.dir` 키 신설(기본 `~/.simpleclaw/recipes`) + `load_recipes_config()`, `discover_recipes(recipes_dir, legacy_dir=…)` 가 primary→legacy 폴백 + 한 번 deprecation 경고, `CronScheduler.__init__` 가 `recipes_dir`/`legacy_recipes_dir` 받아 `_execute_action` 의 하드코드 `.agent/recipes/{name}/recipe.yaml` 제거, `AgentOrchestrator` 가 부팅 시 `recipes_dir` 절대 경로 풀이 + `try_recipe_command` 에 주입, `run_bot.py` 가 `load_recipes_config` 결과를 `CronScheduler` 에 wiring. 마이그레이션 스크립트 `scripts/migrate_recipes_dir.py`(dry-run 기본, `--apply`/`--symlink`/`--force`) 로 기존 4개(`ai-report`/`check-email`/`krstock`/`morning-briefing`) 1회 이전. AGENT.md 의 Directories 섹션 갱신(`~/.simpleclaw/recipes/<name>/recipe.yaml` 명시 + 레거시 deprecation). 단위 테스트 14개(`test_config.TestLoadRecipesConfig` 5개, `test_recipe_loader.TestDiscoverWithLegacyFallback` 5개, `test_recipes_dir_wiring` 5개, `test_scheduler.TestRecipeDirResolution` 4개) (2026-05-15)
- [x] **BIZ-190: wikidocs.net/npmjs.com SPA 회수 max-iter 잔존 패턴 봉합** — BIZ-187 패치(180s 화이트리스트 + composite 분해 가이드) 적재 후에도 wikidocs.net 4건 + npmjs.com 1건이 `web_fetch` 짧은 응답(Cloudflare 차단 페이지, 27~202 chars) 받은 뒤 `agent-browser`/`cli`/`execute_skill` 우회 시도로 max-iter(10) 소진. 봉합 3종: (1) `handle_web_fetch` 가 정적+헤드리스 양 경로 결과를 `_looks_like_block_page`(< 400 chars 또는 cloudflare/just-a-moment 시그니처) 로 검사해 `FETCH_BLOCKED:` 마커로 합성 응답, (2) `_execute_command` 가 ``agent-browser`` + ``&&``/``||``/``;`` composite chain 을 subprocess 진입 전 차단하고 단일-호출 안내 반환, (3) `_tool_loop` 가 같은 turn 안의 ``agent-browser`` 호출 횟수를 카운트해 cap(2) 초과 시 합성 응답으로 즉시 종결. `_GUARD_WEB_FETCH_PREFERRED` 에 `FETCH_BLOCKED:` 마커 대응 + 재시도 금지 한 줄 추가. 단위 테스트 16개(`test_agent.py` 4개: 블록 페이지 감지/시그니처/짧은 본문/회귀 가드; `test_orchestrator_skill_dispatch.py` 9개: composite 판별·블록·call invoker·프롬프트 가드; `test_orchestrator_tool_loop.py` 2개: cap 초과·이내 dispatch) (2026-05-13)
- [x] **BIZ-167: agent-browser `networkidle` 60초 timeout 봉합** — `_fetch_headless` 의 wait 단계를 `--load networkidle` (timeout 30s) → `--load load` (timeout 8s) 로 교체해 wikidocs.net 등 background polling 이 끊임없이 도는 SPA 에서 wait 가 영영 settle 안 되는 사고를 차단. timeout 시에도 `get text body` 가 호출돼 부분 본문 회수 동작은 그대로 유지. `_TOOL_USAGE_INSTRUCTION` 에 `_GUARD_WEB_FETCH_PREFERRED` 한 줄 추가 — 본문 회수는 `web_fetch` 가 디폴트, `agent-browser` composite 명령은 상호작용(클릭/폼/스크린샷) 한정, 부득이 호출 시 `wait --load load` 사용을 명시. 회귀 가드 단위 테스트 3개(`test_wait_uses_load_strategy_not_networkidle`, `test_wait_timeout_does_not_block_text_retrieval`, `test_tool_usage_instruction_prefers_web_fetch_over_agent_browser`) (2026-05-12)
- [x] **BIZ-16: Subprocess 좀비 프로세스 정리 및 타임아웃 보강** — `kill_process_group` SIGKILL 폴백 검증, `waitpid(WNOHANG)` 회수, 그룹 잔존 폴링, 좀비/누수 메트릭, 행 걸린 자식 통합 테스트, `MetricsCollector` 운영 배선(`run_bot.py`) + 대시보드 카드 노출 (2026-05-01)
- [x] **BIZ-29: 임베딩/클러스터 색인 분포 모니터링 및 토큰 절감 측정** — `ConversationStore` 분포 헬퍼(`count_with_embedding` / `embedding_dimension_distribution` / `cluster_member_counts` 등), `simpleclaw.memory.stats`(분포·RAG 로그 집계), `scripts/inspect_memory.py` CLI(텍스트/JSON), `_retrieve_relevant_context()` 구조화 로그(`rag_retrieve` action), 대시보드 `/api/memory_stats` + Memory Index 카드 (2026-05-01)
- [x] **BIZ-25: 구조화 로깅에 Trace ID 도입** — `simpleclaw.logging.trace_context`(contextvars 기반 trace_id 발급/전파/주입), `LogEntry.trace_id` 필수 필드 + 자동 컨텍스트 채택, `process_message`/`process_cron_message` 진입점 발급, 스킬 executor·서브에이전트 spawner의 `SIMPLECLAW_TRACE_ID` 환경변수 전파, 대시보드 `/api/logs?trace_id=…` 필터 + `/api/trace` 타임라인 + Trace Timeline 카드 (2026-05-01)
- [x] **BIZ-15: DB 스키마 마이그레이션 시스템 구축** — `simpleclaw.db.MigrationRunner`(파일 기반 SQL, schema_version 메타 테이블, 베이스라인 흡수, 적용 전 자동 백업·실패 시 원복), conversations/daemon DB의 베이스라인 0001 마이그레이션 도입, `ConversationStore`/`DaemonStore` `__init__`이 부팅 시 자동 적용, 단위 테스트 16개(`tests/unit/test_db_migrations.py`) + GitHub Actions `unit-tests.yml` CI 워크플로 (2026-05-01)
- [x] **BIZ-18: `.env` / API 키 시크릿 매니저 통합** — `simpleclaw.security.secrets`(환경변수/OS keyring/Fernet 암호화 파일 백엔드, ``"scheme:name"`` 참조 문법, 마스터 키 자동 생성 0600 권한·`SIMPLECLAW_MASTER_KEY` 우선), `config.py`의 `api_key`/`bot_token`/`auth_token` 자동 해소(레거시 평문 호환 + 경고 로그), 마이그레이션 스크립트(`scripts/migrate_secrets.py`: `.env`→keyring/file 일괄 이전 + config.yaml 자동 치환), `keyring`/`cryptography` 의존성 추가, `config.yaml.example`/README 가이드 갱신, 단위 테스트 41개(`tests/unit/test_secrets.py`) (2026-05-01)
- [x] **BIZ-24: Webhook 페이로드 크기 제한 및 Rate Limiting** — `WebhookServer`에 `max_body_size`(`Content-Length` 사전 검사 + aiohttp `client_max_size` 이중 안전망, 초과 시 413), 토큰/IP별 슬라이딩 윈도우 rate limit(0이면 비활성, 초과 시 429 + `Retry-After`), `max_concurrent_connections` + `queue_size` 동시성 게이트(포화 시 503 + `Retry-After`) 도입. 비정상 트래픽(연속 차단·단일 IP 폭주·큐 포화)에 대한 알림 콜백 + 쿨다운, `WebhookMetrics` 스냅샷, 차단 사유 `AccessAttempt` 기록 + 선택적 `StructuredLogger` 연계(`webhook_block`/`webhook_alert`). `config.yaml.example`에 신규 키와 튜닝 가이드, 단위 테스트 4종(413/429/503/알림 경로) 추가 (2026-05-01)
- [x] **BIZ-21: 스킬 실행 실패 시 자동 재시도 정책** — `RetryPolicy`(SKILL.md 프론트매터 `retry:` 블록에서 파싱), 멱등성 가드(`idempotent=True` 필요), 지수 백오프(`initial`/`factor`/`max_backoff_seconds`), 타임아웃 재시도 옵트인(`retry_on_timeout`), `execute_skill`에 재시도 루프 + `SkillResult.attempts`, `MetricsCollector`에 `skill_retries`/`skill_retry_recovered`/`skill_retry_exhausted` 카운터 (2026-05-01)
- [x] **BIZ-43: Admin UI 공통 기반 — API 클라이언트 + Layout primitives** — `web/admin/src/lib/api/`(fetchAdmin: Bearer 토큰 자동 주입을 Next 서버측 프록시 라우트 `/api/admin/[...path]`에 위임 + POST/PUT/PATCH Idempotency-Key 자동 부여 + 401/403/422/5xx/네트워크 통합 `AdminApiError`, `useAdminQuery`/`useAdminMutation` SWR 훅, `dryRun(area, patch)` 헬퍼 — BIZ-41 `?dry_run=true` 응답 그대로, `useUndo`/`registerUndo` 5분 윈도우 슬롯), `web/admin/src/components/primitives/`(Modal: focus-trap+ESC, Drawer: slide-from-right, Toast: success/info/warn/destructive-soft + AdminApiError 자동 라우팅 + Undo 액션 슬롯, ConfirmGate: 텍스트 일치 입력 + alertdialog 시맨틱, RestartStepper: 5단계 stepper 모달), CommandPalette ⌘K — 화면 11개 + 설정 키 16개 + 시크릿 메타(이름만) 검색 + ↑/↓ Enter 키보드 흐름. Storybook stories 6종, Vitest+MSW 단위 테스트 18개(`src/lib/api/__tests__/`), DESIGN.md 부록 C(SWR 채택) (2026-05-03)
- [x] **BIZ-48: Admin UI Cron 화면 구현** — `web/admin/src/app/cron/page.tsx`를 Cron 운영 화면으로 채움(잡 테이블·검색/상태 필터·빈 상태·dim 비활성·circuit-break 배지). 표현식 입력기 `_components/ExpressionInput.tsx`(5필드 cron 검증 + 다음 5회 실행 시각 미리보기, 한국어 인라인 에러), 실행 이력 Drawer `RunHistoryDrawer`(최근 20건 + stdout/stderr 펼침), 새 잡 생성 모달 `NewJobModal`(이름/표현식/액션/활성, dry-run 후 저장 게이트). `ConfirmGate` 통과 후 ``Run now`` + 토스트 결과, 삭제는 잡 이름 입력 게이트. BIZ-43가 합류하기 전까지 모달/드로어/토스트/컨펌 게이트 primitives는 `_primitives/`에 라우트-비공개로 동거, 데이터 계층은 `lib/cron/{types,expression,client}.ts`(mock 클라이언트, BIZ-41 백엔드 합류 시 `fetchAdmin` 교체) (2026-05-03)
- [x] **BIZ-52: Admin UI Logging & Traces 화면 구현** — `web/admin/src/app/logs/page.tsx`를 로그 스트림 화면으로 채움. 레벨(debug/info/warn/error)·모듈(`action_type` substring)·자유 텍스트 검색 필터, 1초 폴링 자동 새로고침 토글(시각적 fade-in 강조), `VirtualLogList`로 1만+ 항목 가상 스크롤, "더 보기" 단계 limit 증가(200씩), 행 클릭 시 `TraceDrawer`로 같은 trace_id로 묶인 항목 타임라인 + 선택 항목 JSON 원문(복사 버튼). 모든 필터·trace_id를 URL 쿼리스트링에 동기화해 깊은 링크 공유 가능. `LogLevelControl`은 `/admin/v1/config/logging` PATCH 자리를 미리 마련 — 백엔드 미합류(404) 시 "준비 중 (BIZ-37 후속)" 배지로 명시. `web/admin/src/lib/api/logs.ts` 단위 테스트 15개 (2026-05-03)
- [x] **BIZ-54: Admin UI System 화면 구현** — `web/admin/src/app/system/page.tsx`를 5개 카드(시스템 정보·재시작 액션·서브시스템 헬스·config 스냅샷·테마)로 채움. 신규 백엔드 엔드포인트 `GET /admin/v1/system/info`(version/build_sha/PID/uptime/config·DB 경로/디스크 사용량) 추가. 재시작은 ConfirmGate(`RESTART`/`RESTART-PROCESS` 토큰 일치) → RestartStepper(5단계) → `/admin/v1/system/restart` 호출 흐름. config 덤프는 백엔드 `_mask_secrets`가 적용된 채로 노출(읽기 전용 + 복사 버튼). 헬스는 5초 폴링 `/admin/v1/health` + LLM은 `/admin/v1/config/llm`의 default 프로바이더 유효성으로 약식 판정. 테마 카드는 기존 `ThemeProvider`(라이트/다크/시스템) 라디오. 테스트: 백엔드 `tests/unit/test_admin_api.py`에 system_info 인증·필수 키 케이스 2개, 프런트 `web/admin/src/app/system/_components/__tests__/format.test.ts` 6개 (2026-05-03)
- [x] **BIZ-41: Admin Backend REST API + 감사 + dry-run** — `simpleclaw.channels.admin_api.AdminAPIServer`(aiohttp, `127.0.0.1:8082`, Bearer 토큰 인증, `/admin/v1/{config,secrets,audit,logs,health,system}` 엔드포인트), `admin_audit.AuditLog`(JSONL 일별 로테이션, 시크릿 키 자동 마스킹, ID/필터 검색), `admin_policy`(Hot/Service-restart/Process-restart 분류 + 영역별 검증), Process-restart 변경은 `~/.simpleclaw/admin/pending_changes.yaml`로 적재 후 `/system/restart`에서 머지, 시크릿 reveal 15s TTL nonce, 마스터 키 회전 + 모든 file 시크릿 재암호화, undo 라운드트립으로 새 audit 항목 생성. 단위 테스트 40개(`tests/unit/test_admin_api.py`) + 통합 테스트 6개(`tests/integration/test_admin_audit.py`) (2026-05-03)
- [x] **BIZ-55: Admin UI a11y · 성능 측정 + Lighthouse 95+ 보강** — `Shell`에 "본문으로 건너뛰기" skip-link(`sr-only` → 포커스 시 좌상단 노출, `<main id="main-content" tabIndex={-1}>` 타깃) 도입, `web/admin/lighthouserc.json`(11개 라우트 측정, a11y `error >= 0.95` / 성능·BP `warn` / LCP·CLS·TBT 임계 어설션), `.github/workflows/admin-lighthouse.yml`(treosh/lighthouse-ci-action@v12 — `web/admin/**` 변경 시만 트리거, 임시 공개 스토리지 보고서 + PR 코멘트), `npm run lhci` 로컬 진입점, DESIGN.md §10 부록 D(키보드 시나리오 5종 + VoiceOver 5점검 + 회귀 차단 흐름) 추가 (2026-05-03)
- [x] **BIZ-132: persona/dreaming 라이브 파일 untrack 사고 재발 방지** — `simpleclaw.memory.safety_backup.SafetyBackupManager`(매 dreaming 사이클의 preflight 직전 위험 파일 9종 + DB 2종을 `.agent/_safety_backup/{ts}/` 에 통째 스냅샷, 보존 정책 `최근 7개 + 최근 1 always`, SQLite 는 `Connection.backup` API 로 atomic 복사), `DreamingPipeline._preflight_protected_sections` 좁은 자가 복원(파일 *부재* 한정 1회 — safety backup → legacy `memory-backup/` 폴백, 마커 손상은 그대로 abort), 복원 발생 시 `dreaming_runs.jsonl` 의 `details.recovered_from` 기록 + WARN 로그, `scripts/run_bot.py` 매니저 배선. 단위 테스트 21개(`test_safety_backup.py` 14개 + `test_dreaming_self_restore.py` 7개) (2026-05-05)
- [x] **BIZ-138: dreaming 경로 회귀(`.agent/` → `~/.simpleclaw/`) 봉합** — `scripts/run_bot.py` 의 SafetyBackupManager wiring 이 BIZ-133 후에도 ``.agent/AGENT.md`` 등 9 종 + ``.agent/_safety_backup`` backup_root 를 하드코드로 들고 있어, config.yaml 이 ``~/.simpleclaw/`` 를 가리키는데도 봇이 ``.agent/`` 에 빈 백업 디렉터리만 만들고 dreaming preflight 가 ``.agent/MEMORY.md`` 를 못 찾아 abort 하던 회귀를 봉합. 페르소나 4 종은 ``persona_local_dir / "X.md"`` 로, sidecar/HEARTBEAT/DB/backup_root 는 모두 config 키(``daemon.dreaming.{insights,suggestions,blocklist,runs}_file``, ``daemon.status_file``, ``daemon.dreaming.safety_backup_dir``) + ``_expand`` 헬퍼로 라우팅. 회귀 가드 단위 테스트 8개(`tests/unit/test_bot_wiring_paths.py` — 소스 리터럴 검사 2 + config 기본값 검사 3 + wiring 시뮬레이션 검사 2 + override 라운드트립 1), `.agent/` 잔여물 격리 스크립트 `scripts/cleanup_legacy_agent_dir.py`(dry-run 기본 + ``--apply``, 라이브 파일·디렉터리·마이그레이션 사이드카만 격리, ``skills/``·``recipes/`` 보존) + 단위 테스트 5개(`tests/unit/test_cleanup_legacy_agent_dir.py`) (2026-05-06)
- [x] **BIZ-76: Cron/Recipe 메시지 코퍼스 분리** — `ConversationMessage.channel`에 표준 prefix 규약(`recipe:<name>`, `cron:`, `cron-admin`) 도입 + 단일 분류기 `is_auto_trigger_channel` (`memory/models.py`), orchestrator 가 `/cron *` 응답을 `cron-admin`, `/<recipe>` 산출물을 `recipe:<name>` 으로 태깅 (`agent/orchestrator.py`, `agent/commands.py`), DreamingPipeline 코퍼스 로더에 3-mode 필터(`exclude` 기본 / `downweight` stride sampling / `include` 레거시) — 알 수 없는 모드는 fail-closed `exclude` 폴백, weight 클램프 [0,1], 시간순 보존. DoD: auto-trigger only 코퍼스 → `run()` 이 None 반환 + LLM 미호출. 단위 테스트 18개(`tests/unit/test_dreaming_cron_recipe_filter.py`) (2026-05-03)

---

## Backlog

### 에이전트 코어
- [ ] `/dreaming` 수동 트리거 명령어 — Telegram에서 dreaming을 수동 실행할 수 있도록
- [ ] LLM 라우터 hot reload — config.yaml의 LLM 프로바이더/모델 변경 시 재시작 없이 반영
- [ ] 대화 히스토리 정리 기능 — 오래된 대화 자동 삭제 또는 아카이브
- [ ] OpenAI 호환 API 지원 — OpenAI API 형식을 따르는 서드파티 LLM(vLLM, Ollama, LM Studio 등) 연동
- [ ] Claude Code CLI 연동 — Claude Code CLI를 SimpleClaw의 백엔드 또는 도구로 활용
- [ ] Claude, ChatGPT API 지원 — Claude/ChatGPT를 기본 LLM 백엔드로 실사용 가능하도록 검증 및 보완

### 시맨틱 메모리 (spec 005, BIZ-12)
- [x] **Phase 1** — `ConversationStore` 임베딩 저장/검색 API + 단위 테스트 (PR ingki3/simpleclaw#16, merged 2026-04-26)
- [x] **Phase 2** — `EmbeddingService` 통합, `_retrieve_relevant_context()` + `_tool_loop()` 하이브리드 회상 (PR ingki3/simpleclaw#17)
- [>] **Phase 3** — 그래프형 드리밍, `semantic_clusters` 인덱스, `MEMORY.md` 마커 기반 자동 upsert, 에피소드/시맨틱 분리

### 스킬
- [ ] us-market-expert 스킬 재구축 — 실행 스크립트 추가 (현재 삭제됨)

### 레시피
- [ ] 레시피 목록 조회 명령어 (`/recipes`) — 등록된 레시피 목록 확인
- [ ] 레시피 v1(step 기반)에서 v2(instruction 기반)로 마이그레이션 가이드

### Cron
- [ ] Cron job 실행 히스토리 조회 — 최근 실행 결과 확인 명령어
- [ ] Cron job 자연어 수정 — "메일 확인 주기를 1시간으로 바꿔줘"
- [x] **BIZ-19: Cron 작업 실패 시 자동 재시도** — `CronJob`에 작업별 재시도 정책(max_attempts/backoff_seconds/backoff_strategy/circuit_break_threshold) + `consecutive_failures` 카운터, `CronJobExecution.attempt` 컬럼으로 재시도별 실행 기록 분리, `execute_job()` 재시도 루프(linear/exponential 백오프), 누적 실패 임계값 도달 시 자동 비활성+알림 콜백, `enable_job()`이 카운터 리셋 (2026-05-01)

### 인프라
- [ ] 프로세스 매니저 도입 — systemd 또는 supervisord로 봇 안정적 운영
- [ ] 로그 로테이션 — bot.log 파일 크기 관리
- [x] **CI/CD** — GitHub Actions로 PR/푸시 시 단위 테스트(+DB 마이그레이션) 자동 실행 (BIZ-15에 포함, 2026-05-01)
- [>] **BIZ-172: Feature PR 자동 base-sync** — `.github/workflows/pr-base-sync.yml`(on push: dev) — base=dev 인 OPEN PR 의 behind_by 조회, ≥ 1 이면 `needs-rebase` 라벨 + 1회 안내 코멘트, 따라잡으면 라벨 자동 제거. `merge/main-to-dev-*` 패턴은 제외. 라벨은 워크플로가 멱등 생성(`gh label create --force`), 권한 `pull-requests: write` + `issues: write` (라벨)
- [ ] **서비스 모니터링 구성** — 1) 대시보드(`/api/metrics`) 임계치 알림(텔레그램), 2) `process_group_leaks > 0` 또는 좀비/자식 PID 단조 증가 시 자동 경보, 3) BIZ-16 후 1주 집중 관찰을 자동 스크립트로 대체. (BIZ-16에서 분리 — 후속 이슈로 등록)

### 문서
- [ ] docs/ MkDocs 또는 Docusaurus 기반 정적 사이트 생성
- [ ] 스킬 개발 튜토리얼 — 처음부터 스킬 만들기 가이드

---

## Done

### 2026-08-08

- [x] **[BIZ-642](mention://issue/f5c8b31e-46a5-4ef7-97c8-435a57885a01): visible citation canonical subset으로 실제 KBO gate 통과** — provider가 반환한 valid concrete citation을 response content에 보이는 path만으로 결정적으로 축소하는 code/offline 범위를 [PR #697](https://github.com/ingki3/simpleclaw/pull/697)로 완료하고 squash merge(SHA `fbd7b472b7c25b664098d896fb769ee0e744aad3`)했다. 이 완료는 [BIZ-641](mention://issue/9b359807-b364-40e8-a646-81a4bcd93307)이 별도 소유하는 release·production activation·restart·Telegram smoke 완료를 뜻하지 않으며, 부모 BIZ-641은 계속 진행 중이다. (완료: 2026-08-08)


### 2026-08-07

- [x] **[BIZ-630](mention://issue/c5650d41-78eb-4f0a-9856-55a6cdbdae81) / [BIZ-631](mention://issue/57811dcb-b92e-48de-9157-a2170101b7cb) / [BIZ-632](mention://issue/133b7424-91c1-46cd-8fa2-586ed8dbb066) / [BIZ-633](mention://issue/155ed327-4f9f-46c9-bdbf-9146547db4bf): 중앙 composer hard-deadline 체인을 완결** — BIZ-630/631 공유 [PR #687](https://github.com/ingki3/simpleclaw/pull/687)은 squash merge(SHA `dafc56e7ee77171b9b33c73b2bd81426a88b72eb`)됐고, BIZ-633 corrective [PR #688](https://github.com/ingki3/simpleclaw/pull/688)은 exact HEAD `878e7c5d46bc4dc78b38405ca0967999d344f1ce`에서 반복 probe `50/50`, focused `109 passed`, fresh CI 3/3과 독립 Stage D를 통과해 squash merge(SHA `144e8d1aa8660eec7f0359e1e89c26bada6296ea`)됐다. 이로써 timeout 단독 exactly-once 회귀의 deadline을 composer 진입 뒤 작동하도록 동기화한 code/offline lifecycle은 완료됐다. 후속 [BIZ-637](mention://issue/d2ce1762-645b-400c-8e2b-e044e26792a9) corrective [PR #691](https://github.com/ingki3/simpleclaw/pull/691)은 exact reviewed HEAD `37cce9ef4bc2b8cafd93893b2f0a67ba2dd0d78d`에서 Linux CI의 compose/guard/record timeout 테스트 계측을 대상 await 내부의 deterministic reschedule로 보정하고 제품 deadline 경계는 변경하지 않은 채 fresh CI 3/3과 독립 Stage D를 통과해 squash merge(SHA `837e7d23feae8539fa72360f277a267b8150b97c`)됐다. 리뷰 중 생성된 [BIZ-638](mention://issue/afe5f1c7-cfe7-4a24-9b21-51b5c7d8f911) / [PR #692](https://github.com/ingki3/simpleclaw/pull/692)는 철회된 제품 변경과 함께 superseded되어 이슈 `done`, PR `CLOSED`·unmerged(`mergedAt=null`, `mergeCommit=null`)로 종료됐으며, 이에 따라 BIZ-632 current-`dev` 최종 Stage D 재평가의 repository gate가 해제됐다. 중복 BIZ-636/PR #690은 재개하지 않는다. Production release·deploy·restart·Telegram smoke는 이 완료와 분리된 별도 gate로 계속 보류하며 본 체인에서는 수행하지 않는다. (완료: 2026-08-08)

- [x] **[BIZ-627](mention://issue/10515293-4f91-4128-af2b-389b8b549e59): Post-main Unit Test catalog assertion을 결정적으로 수정** — whole-prompt 숫자 substring 검사를 parsed JSON의 bounded capability identity·fingerprint 검증으로 대체하고, excluded `skill:calendar` 비노출과 정상 timestamp `2026-08-07T01:02:03.101102+00:00` 허용, 실제 `msg:101` identifier leak fail-closed를 고정했다. formerly failing test `20/20`, raw identifier leak·file suite `21 passed`, 전체 unit `3588 passed, 3 xfailed`, Ruff·diff check와 fresh exact-head CI 3종 및 독립 Stage D를 통과한 [PR #683](https://github.com/ingki3/simpleclaw/pull/683)을 squash merge(SHA `3e3155bceba3c32b5658ebe7f22cba150efa13bc`)했다. 본 완료는 code/offline corrective 범위이며 새 release의 post-main 4종 성공 전까지 [BIZ-573](mention://issue/d91645fd-f0de-48b9-a10f-7469773f827c) production deploy·Telegram smoke HOLD를 유지한다. (완료: 2026-08-07)

- [x] **[BIZ-626](mention://issue/4b4b7058-ba45-45f7-92a5-86996d3a38a1): Telegram persistence SQLite 호출을 event loop에서 격리** — ConversationStore read/bind/write를 `asyncio.to_thread()`로 event-loop 밖에 격리하고 실제 SQLite write lock 중 heartbeat 지속, lock 해제 뒤 send/user/assistant/outbound marker exactly-once, replay duplicate 0과 retry 소진 fail-closed를 검증했다. focused `16 passed`, changed-file Ruff·diff check와 fresh exact-head CI 3종 및 독립 Stage D를 통과한 [PR #678](https://github.com/ingki3/simpleclaw/pull/678)을 squash merge(SHA `84f597e005338d1357707b771e70f24615ed5c5d`)했다. 부모 [BIZ-624](mention://issue/56fb8f9b-d22d-4c55-b183-2de3022cb9b7)는 fresh 재평가 전까지 `in_review`, [BIZ-573](mention://issue/d91645fd-f0de-48b9-a10f-7469773f827c) release/deploy/smoke는 HOLD를 유지하며 실제 Telegram 전송·live config·restart/deploy는 수행하지 않았다. (완료: 2026-08-07)

- [x] **[BIZ-624](mention://issue/56fb8f9b-d22d-4c55-b183-2de3022cb9b7): V4 primary Telegram delivery 후 assistant persistence를 exactly-once 보장** — 원래 Telegram request/turn ID에 assistant row를 저장하고 delivery receipt를 재사용하는 bounded persistence-only retry, retry 소진 typed failure와 polling false-green 차단을 구현했다. 단일 polling update에서 Telegram send 1 / user 1 / assistant 1을 검증하고, late required finding은 corrective [BIZ-626](mention://issue/4b4b7058-ba45-45f7-92a5-86996d3a38a1)에서 SQLite marker read·legacy bind·assistant write를 event-loop 밖으로 격리해 lock 중 heartbeat와 replay exactly-once를 보완했다. [PR #675](https://github.com/ingki3/simpleclaw/pull/675)을 squash merge(SHA `e753fd0e7bf3d7a94508bc40168a321d85c3396a`)하고 corrective [PR #678](https://github.com/ingki3/simpleclaw/pull/678)을 squash merge(SHA `84f597e005338d1357707b771e70f24615ed5c5d`)한 뒤 fresh CI 3종과 독립 Stage D 재평가를 통과했다. 본 완료는 code/offline 범위이며 [BIZ-573](mention://issue/d91645fd-f0de-48b9-a10f-7469773f827c)의 release/main merge/live deploy/restart/V4 activation/Telegram smoke 승인이 아니다. (완료: 2026-08-07)

- [x] **[BIZ-623](mention://issue/c908d032-3e2c-4b51-8ce0-047bca3391e1): Sports Live explicit top-N bound를 production argument path에 보존** — 사용자 질의의 explicit top-N을 Recipe bound payload, canonical helper argv, typed result와 renderer까지 보존하고 parser-equivalent `--limit=…` 및 abbreviation override를 dispatch 전과 trace 사후에 fail-closed했다. production parser의 abbreviation을 비활성화하고 connected adversarial 회귀, 관련 unit `84 passed`, exact-head CI 3종과 독립 Stage D를 통과한 [PR #676](https://github.com/ingki3/simpleclaw/pull/676)을 squash merge(SHA `9ba0e5b27bd9a09fe9018a39c95ea1082e2942ea`)했다. 본 이슈는 code/offline corrective만 완료했으며 release·install·live no-send·Telegram smoke는 수행하지 않았고, 해당 운영 lifecycle은 부모 [BIZ-573](mention://issue/d91645fd-f0de-48b9-a10f-7469773f827c)이 소유한다. (완료: 2026-08-07)

- [x] **[BIZ-622](mention://issue/5f5aa8b7-65c9-4618-be0e-afa21d01dc4c): Production asset validator를 contract stub과 actual execution으로 분리** — contract-only harness에 stub scope와 `HELPER_CLI_EXECUTED=false`를 명시하고, 별도 installed-helper gate에서 documented `--season auto` argv를 wrapper→CLI parser→exact checkout canonical helper로 실행하도록 분리했다. Ambient venv·`PYTHONPATH`를 차단하는 isolated source path/SHA provenance, deterministic 및 real read-only preflight, sentinel·bound-args drift fail-closed mutation, canonical source payload→mapped Skill args→production exact Recipe boundary→typed result→renderer 연결을 고정했다. 독립 Stage D에서 focused `125 passed`, deterministic item 3 및 real item 10, `side_effect=false`, external write `0/0/0`, Ruff·diff check·Graphify provenance와 fresh exact-head CI 3종을 확인한 [PR #670](https://github.com/ingki3/simpleclaw/pull/670)을 squash merge(SHA `0d9d81a82288fa8245f1deaa5576a024cc52c05f`)했다. Release·live deploy/restart·V4 activation·Telegram 전송은 수행하지 않았으며 후속 release/live no-send/controlled smoke는 [BIZ-573](mention://issue/d91645fd-f0de-48b9-a10f-7469773f827c)이 소유한다. (완료: 2026-08-07)

- [x] **[BIZ-621](mention://issue/fd4e41f5-659c-4156-9ab1-f4cbd8e76ac9): Sports Live `season=auto` 계약을 active season 선택과 정렬** — 문서화된 `--season auto` sentinel을 latest enabled/active season 선택으로 정규화하고 explicit unknown의 `INVALID_ARGUMENT`, LCK `lck_auto` 비생성, exact recipe 단일 delegate, connected V4 bounded top-three/no-send 회귀를 고정했다. 전체 unit `3562 passed, 3 xfailed`, contracts `41 passed`, relevant integration `72 passed`, Ruff·diff check, Graphify `15363 nodes / 34111 edges`, fresh exact-head CI 3종과 독립 Stage D를 통과한 [PR #668](https://github.com/ingki3/simpleclaw/pull/668)을 squash merge(SHA `e687557ad0cb418d28873cee6f2d6718bf89b602`)했다. 후속 production-asset gate는 [BIZ-622](mention://issue/5f5aa8b7-65c9-4618-be0e-afa21d01dc4c), release/live no-send/추가 Telegram smoke는 [BIZ-573](mention://issue/d91645fd-f0de-48b9-a10f-7469773f827c)이 소유하며 본 완료 범위에서는 release·live deploy/restart·V4 primary activation·Telegram 전송을 수행하지 않았다. (완료: 2026-08-07)

- [x] **[BIZ-616](mention://issue/00906e74-a78f-4ba3-aa14-b157d7791fc3): V4 connected binding 진단 기대값을 sanitized 계약에 정렬** — fallback 미선언 CLI Skill의 stale `payload.safe_example_missing` 기대값을 closed phase code `connected_binding_failed`로 정렬하고, raw code·질문 비노출과 asset dispatch/Telegram/notifier/ConversationStore write `0/0/0/0`을 회귀로 고정했다. Source `1ce154e24101befcace05490800f96aadae92058`와 Graphify exact head `c83377cb72077abeb705e0e5d67dfbf22f1b6173`에서 user-scenario `16 passed`, 진단·formatter `11 passed`, 전체 Ruff·diff check, fresh CI 3종 및 독립 exact-head 리뷰를 통과한 [PR #660](https://github.com/ingki3/simpleclaw/pull/660)을 squash merge(SHA `6a8efffffefc86ec75a0b05dc284d93269fe9be2`)했다. Production allowlist·live config·deploy·restart·V4 primary·Telegram send는 변경하지 않았다. (완료: 2026-08-07)

- [x] **[BIZ-609](mention://issue/939c2a5c-9e9c-4090-83db-e5672529eeee): V4 primary effective-plan ValueError를 진단·수정** — 원 구현 [PR #639](https://github.com/ingki3/simpleclaw/pull/639) merge 뒤 late mandatory review finding을 corrective children BIZ-610/BIZ-611/BIZ-612에서 각각 formatter redaction, domain-neutral installer/package 경계, catalog→registry TOCTOU 차단으로 보완해 모두 exact-head 리뷰·fresh CI·squash merge했다. Combined 최신 `dev@1a3c8d74a17b28722651b2cb33ebae96cc5240eb`에서 production installer output 기반 KBO no-send 3회, typed final·dispatch exactly-once, contract continuity `2/2`, side effect `0/0/0`, combined regression `137 passed`, 전체 unit `3538 passed, 3 xfailed`, Ruff·diff·CI 4종을 통과해 부모 code/offline corrective 범위를 완료했다. BIZ-573은 release·controlled live activation 전까지 `blocked`, live `legacy_v2`/V4 `no_send`를 유지한다. (완료: 2026-08-06)


### 2026-08-06

- [x] **[BIZ-615](mention://issue/228f1346-0558-44e3-8384-840d1ed43904): schema_version strict-type 검증을 fail-closed로 보정** — runtime asset manifest의 canonical version을 실제 `int` 값 `1`로 제한하고 `True`, `1.0`, 문자열, null, unsupported integer를 destination mutation 전에 거부하며 기존 sentinel을 보존하는 회귀를 추가했다. Exact head `e11aa692828f85c12620dd12c36947c783f58d05`에서 focused `28 passed`, 전체 unit `3544 passed, 3 xfailed`, Runtime Contracts `35 passed`, Offline Integration `99 passed`, Ruff·diff와 fresh CI 3종을 통과했고 reviewed tree와 동일한 [PR #652](https://github.com/ingki3/simpleclaw/pull/652)를 `dev`에 squash merge(SHA `b08dc9d0a6035f6dd5ecaa11da9c3027be3bc4da`)했다. 본 child의 code/offline 범위는 완료됐으며 parent BIZ-611 재승인은 dependency-unblocked 후속 review가 소유한다. Live install·deploy·restart·Telegram send·primary activation은 수행하지 않았다. (완료: 2026-08-06)

- [x] **[BIZ-611](mention://issue/968d342f-5794-4fd3-b1d8-bfe2d085f348): Runtime asset installer 경로와 package artifact를 domain-neutral하게 정렬** — generic manifest/resource resolver와 installer를 canonical 경로로 두고 sports-named CLI를 thin compatibility wrapper로 유지했다. Source/destination symlink containment, catalog manifest provenance, executable boolean schema, complete regular-tree/bytes/mode ownership, post-swap cleanup rollback, package artifact와 runtime discovery 경로를 fail-closed 회귀로 고정했다. [PR #642](https://github.com/ingki3/simpleclaw/pull/642)를 squash merge(SHA `157f7fb2d58363ba7019d707632ab85e6fcba874`)한 뒤 확인된 `schema_version` equality-coercion late finding은 corrective [BIZ-615](mention://issue/228f1346-0558-44e3-8384-840d1ed43904) [PR #652](https://github.com/ingki3/simpleclaw/pull/652)에서 strict `int` 검증과 6-case pre-install 회귀로 보정했다. PR #652 exact head `e11aa692828f85c12620dd12c36947c783f58d05`의 fresh CI 3종 및 독립 focused `28 passed`·Ruff·diff 검증 후 squash merge(SHA `b08dc9d0a6035f6dd5ecaa11da9c3027be3bc4da`)했고, child terminal bookkeeping [PR #654](https://github.com/ingki3/simpleclaw/pull/654) merge(SHA `d32b2b39f7bccd0356ae7269577597bb89faa4ae`)까지 확인해 parent 재승인 gate를 완료했다. Live install·deploy·restart·Telegram send·primary activation은 수행하지 않았다. (완료: 2026-08-06)

- [x] **[BIZ-614](mention://issue/9a5c9aec-d569-4753-9730-d3dee0f57a05): V4 structured asset identity 로그 누출을 차단** — user-managed original/effective/diagnostic asset identity와 invalid definition/catalog fingerprint를 production formatter 경계에서 canonical SHA-256 또는 closed surrogate로 fail-closed했다. exact head `026709dc7546e5a4245de8ed72a4d1df860c1e4e`에서 전체 unit `3510 passed, 3 xfailed`, 관련 contracts/integration `101 passed`, 독립 focused `92 passed`, Ruff와 fresh CI 3종을 통과했으며 marker 0과 canonical provenance 보존을 확인했다. [PR #648](https://github.com/ingki3/simpleclaw/pull/648)을 `dev`에 squash merge(SHA `e0dd62a460b94bfb0b7edcacc67dc6d704e38715`)했고 deploy·restart·live activation·사용자-visible 전송은 수행하지 않았다. (완료: 2026-08-06)

- [x] **[BIZ-610](mention://issue/a6f05858-c411-43a4-af66-d1570da12b05): V4 connected 오류 로그의 raw payload 누출을 차단** — connected 오류 로그의 raw exception/cause와 prompt/provider payload/credential을 digest·closed provenance로 대체한 [PR #641](https://github.com/ingki3/simpleclaw/pull/641)을 squash merge(SHA `a9f942c90ffc4bc21515d75d280f2bff96b5627f`)한 뒤, late review에서 발견된 structured asset identity와 fingerprint fallback 누출을 corrective [BIZ-614](mention://issue/9a5c9aec-d569-4753-9730-d3dee0f57a05)의 [PR #648](https://github.com/ingki3/simpleclaw/pull/648)로 보정해 squash merge(SHA `e0dd62a460b94bfb0b7edcacc67dc6d704e38715`)했다. 두 경로의 fresh CI와 exact-head 독립 security review를 통과했으며 fail-closed promotion·legacy fallback·side-effect 0과 성공 exactly-once를 유지했다. (완료: 2026-08-06)

- [x] **[BIZ-612](mention://issue/736fe0e0-5f12-4b5d-a87a-89c6e53a3b6f): V4 catalog-registry fingerprint TOCTOU dispatch를 차단** — exact selected asset definition fingerprint를 effective plan에 봉인하고 definition/binding/executor-definition drift를 adapter 호출 전에 `approved_asset_fingerprint_mismatch`로 fail-closed했다. combined BIZ-610 redaction 경계를 보존한 exact head `c9650db5087ba8e7c52c45a7889781937d7e7ce7`에서 독립 집중 회귀 `123 passed`, Ruff, diff check 및 fresh CI 3종을 통과했고, [PR #643](https://github.com/ingki3/simpleclaw/pull/643)을 `dev`에 squash merge(SHA `eeea899d763fe47f92cce4a6085587894ae8a4e7`)했다. BIZ-613은 별도 lifecycle로 추적하며 live config·deploy·restart·primary activation은 수행하지 않았다. (완료: 2026-08-06)

- [x] **[BIZ-613](mention://issue/e478ecd4-5c1a-43a7-80af-631e2461b6b6): dev의 drama follow-up freshness 기준선 회귀를 복구** — production freshness 경계 변경 없이 drama follow-up fixture를 현행 PlanGate 계약에 맞춰 `freshness_required=True`로 보정했다. fresh CI 3종과 독립 Stage D exact-head 리뷰를 통과한 [PR #645](https://github.com/ingki3/simpleclaw/pull/645)을 `dev`에 squash merge(SHA `74da3d90e50f6def34e852610bfd5d79ec697c50`)했다. deploy·restart·live activation은 수행하지 않았다. (완료: 2026-08-06)

- [x] **[BIZ-600](mention://issue/d70de3cc-6f7f-441a-a8d7-d6cddb6e64b5): LangGraph V4 true primary/canary wiring을 완성** — `off|shadow|read_only_canary|primary` rollout, V4 typed final/provenance, durable exactly-once target dispatch·delivery·persistence와 unsafe effect/invalid mode/post-dispatch fallback의 fail-closed 경계를 구현했다. 원 기능 [PR #634](https://github.com/ingki3/simpleclaw/pull/634)를 `dev`에 squash merge(SHA `130ead41fc157c417f2fd4f86964ac02b70a4552`)한 뒤, 필수 안전 리뷰 finding은 [BIZ-601](mention://issue/89934d4a-1925-43bd-8c4f-ae7edf2c952b)의 [PR #636](https://github.com/ingki3/simpleclaw/pull/636)으로 보정해 squash merge(SHA `f230b7b533eedd6cc8468792ab901ef67a4c2fda`)했다. Corrective exact head `36ee712bea4d6b14cf6fd0376771a3349bad7fea`에서 구조·안전성 gate, operator detached focused `219 passed`, hermetic contract `4/4`, Ruff/diff와 CI 3종을 통과했고 external provider 및 Telegram/Cron/ConversationStore 호출은 `0/0/0/0`, rollback false였다. Live config 변경·release·restart·사용자-visible send와 post-restart primary smoke는 별도 후속 [BIZ-573](mention://issue/d91645fd-f0de-48b9-a10f-7469773f827c)이 단독 소유한다. (완료: 2026-08-06)

- [x] **[BIZ-580](mention://issue/265ea749-7e47-4ea2-a6e1-226a2a12fe6d): V4 route와 execution mode 안정성을 개선** — 실제 production `unified_turn_planner@11`/examples@8에 contract 우선순위와 최소 deterministic asset scope를 보강하고, runtime-visible declared asset만 provider schema에 노출하며, 안전한 full Skill의 중복 supporting scope와 독립 질문 원문 손실을 구조적으로 정규화했다. Evaluator가 native tool contract를 connected probe에도 동일하게 전달하도록 조립 경계를 고정했다. 최종 authoritative corrected `25 cases / 39 runs`는 **GO**이며 schema 100%, route+mode+context macro 97.4%, critical pass/stability 100%/100%, connected 31/31, contract gap·inventory fidelity·rollback 0, Telegram/Cron/ConversationStore `0/0/0`이다. amended 자식 BIZ-585/586/591/592/593/596/597을 모두 완료하고 [PR #629](https://github.com/ingki3/simpleclaw/pull/629)를 squash merge(SHA `0f31349aceee9a02c0d7375082652cccdc2dfd70`), [PR #630](https://github.com/ingki3/simpleclaw/pull/630)을 squash merge(SHA `52dc20240620623457c5e5edada36b094d17c24a`)했다. [BIZ-573](mention://issue/d91645fd-f0de-48b9-a10f-7469773f827c)은 live activation 승인 전 `blocked`를 유지하며 deploy·restart·live activation·사용자-visible 전송은 수행하지 않았다. (완료: 2026-08-06)

### 2026-08-05

- [x] **[BIZ-585](mention://issue/76617451-aab7-40b2-8780-32b781844f3c): sports-live Recipe의 owned contract를 완성** — source `969f60fce2bfe634d2aa713f4098c8fe0c28501c`에서 `recipe:sports-live` 소유 `query` input·JSON object output contract와 `query→naver-sports-skill.args` binding을 고정했다. discovery→planner catalog→Contract Registry identity/hash 연속성, metadata 누락·owner mismatch의 executor dispatch 0, fq-05~07 exact Recipe의 connected `completed`·rollback false·side effects `0/0/0`을 검증했다. contracts `21 passed`, V4 integration `12 passed`, 전체 unit `3434 passed, 3 xfailed`, Ruff와 exact-head CI 3종 `SUCCESS`를 통과했다. Graphify/product/test 파일과 live 설치·restart·배포·사용자-visible 전송의 추가 변경 없이 [PR #619](https://github.com/ingki3/simpleclaw/pull/619)을 squash merge(SHA `4d5c7ba74732cffc4b99ea60cc2b4fbda54164d3`)했다. (완료: 2026-08-05)
- [x] **[BIZ-564](mention://issue/db4158c9-8318-4322-8a5c-1384c8e35919): LangGraph V4 dependencies와 domain-neutral contracts 추가** — 요청·자산·계획·결과·artifact·delivery의 도메인 중립 V4 계약과 bounded config를 추가하고, canonical JSON 방어적 복사로 중첩 mutation과 hash 분리를 차단했다. 내부 `schema_version` + wire alias `schema`로 Pydantic shadow 경고를 제거했으며 task-owned 한국어 docstring, warnings-as-errors·hash continuity 회귀, Graphify 공유 산출물을 보강했다. focused `21 passed`, 전체 unit `3318 passed, 3 xfailed`, Ruff 0.16.1, 필수 CI 3종과 독립 Stage D 검증을 통과했고 [PR #593](https://github.com/ingki3/simpleclaw/pull/593)을 `dev`에 squash merge(SHA `0f14700681140b798d4b53f7a41051e4465b6f67`)했다. graph builder/node/reducer/checkpoint/delivery 실제 구현과 live config/restart/외부 전송은 후속 이슈 범위로 유지했다. (완료: 2026-08-05)

### 2026-08-02

- [x] **[BIZ-535](mention://issue/9bbe3666-ed26-4926-a70e-b605d89c0dca): OpenRouter Gemini UnifiedTurnPlan schema 호환 — 코드·Graphify·live 후속 완료** — [PR #553](https://github.com/ingki3/simpleclaw/pull/553)을 `dev`에 squash merge(SHA `e26169e7418f7731abeb0bcceddd5a8dbcd8084d`)해 OpenRouter profile 경계의 schema 복사본에서만 upstream 비호환 `maxItems`·`propertyOrdering`을 제거하고 provider-neutral 원본 schema와 parser clamp를 보존했다. generated-only Graphify [PR #554](https://github.com/ingki3/simpleclaw/pull/554)도 `dev`에 squash merge(SHA `08698e2a0ee4201e25de0506af98eca83b6885e3`)했으며, 후속 BIZ-532의 release PR #555와 `primary`/`legacy_v2` restart·health·no-send 3/3까지 완료됐다. (완료: 2026-08-02)

- [x] **[BIZ-532](mention://issue/08da18c2-4a3d-4c6b-802a-acc0f8160fe1): Turn/session state final release와 live rollout 완료** — [release PR #555](https://github.com/ingki3/simpleclaw/pull/555)를 `main ← dev` merge commit(SHA `fb5490b8f3255e8a6b073f77dd10c8e9558531e7`)으로 반영해 tag `v2026.08.02.1`을 발행했다. live checkout 동기화와 timestamp config backup 후 planner `primary`, architecture `legacy_v2`로 restart했으며 Admin·Telegram·Webhook·Scheduler health, 신규 오류 0건, pre/post actual-provider no-send 각각 schema·PlanGate·`answer_with_evidence` 3/3, Telegram send 0건을 확인했다. (완료: 2026-08-02)

- [x] **[BIZ-527](mention://issue/cc0bf8e4-7883-4053-852c-f39637e37d04) / [BIZ-528](mention://issue/4c14d104-1ced-498d-991f-5d1603ca92af): Turn/Session/Evidence gate 코드·release·live 검증 완료** — PR #537을 `dev`에 squash merge(SHA `d5005bddeb6ceba9a07e6a92b75c2858c06c61e7`)해 typed realtime request, verified-evidence finalization, required-claim capability 검증을 반영했고, late-finding 보정 BIZ-529/PR #544와 정책 보정 BIZ-531/[PR #546](https://github.com/ingki3/simpleclaw/pull/546) squash SHA `867c9c467aaf9d334a43186b685eb3fc588c6b76`으로 canonical 이력을 완료했다. 초기 release PR #552/tag `v2026.08.02` 이후 BIZ-535/PR #553으로 OpenRouter schema blocker를 해소했으며, BIZ-532의 [release PR #555](https://github.com/ingki3/simpleclaw/pull/555) merge SHA `fb5490b8f3255e8a6b073f77dd10c8e9558531e7`, tag `v2026.08.02.1`, `primary`/`legacy_v2` restart·health·pre/post no-send 3/3으로 terminal live 검증을 완료했다. (2026-08-02)

- [x] **[BIZ-524](mention://issue/529b497b-ae11-4e82-818c-74fa36200f0d): 모든 Gemini 호출을 OpenRouter로 전환** — [PR #541](https://github.com/ingki3/simpleclaw/pull/541)을 `dev`에 squash merge(SHA `ef0dffb9d6f2677848d317eaf59ada5059f57af3`)하고 [release PR #542](https://github.com/ingki3/simpleclaw/pull/542)를 `main`에 merge commit(SHA `e3757f88d185c0d4f6aa7ff60967cf81058b8d53`)으로 반영해 `v2026.08.01.1`을 발행했다. live config backup 후 TurnAnalysis·Asset Selector·default retry·multimodal core route를 OpenRouter `google/gemini-3.6-flash`로 전환하고 LaunchAgent를 재시작했다. text/default·required structured·forced tool·image·PDF no-send, Admin UI·Webhook HTTP 200, Telegram polling·scheduler 정상, Telegram send/edit 0건, ERROR 0건, SimpleClaw core native Google Gemini 호출 0건을 확인했다. (완료: 2026-08-02)

### 2026-07-30

- [x] **[BIZ-520](mention://issue/c30858d5-f1fa-40ee-a883-99852fbaf6b5): evidence_required를 실행·최종 gate에 연결** — [PR #534](https://github.com/ingki3/simpleclaw/pull/534)를 `dev`에 squash merge(SHA `84ab7744`)해 evidence relevance·claim coverage·source·freshness 검증, typed empty/schema failure 분리, truncated analysis fail-closed, asset-owned provider 실행, untrusted evidence 경계를 코드·오프라인 테스트로 완료했다. Live config·배포·재시작은 [BIZ-497](mention://issue/599386f6-1aa4-4d1f-85a4-68d804342851) 운영 gate가 소유한다. (2026-07-30)
- [x] **[BIZ-521](mention://issue/3a00bfdf-1463-40ab-842b-e481ddb367b2): PR #534 evidence gate review blocker 해소** — required review의 blocker 재현 7건과 actual unified·legacy 2턴 회귀를 보완하고 focused `158 passed`, Ruff, `git diff --check`, GitHub CI 3개 SUCCESS를 확인한 뒤 [PR #534](https://github.com/ingki3/simpleclaw/pull/534)를 `dev`에 squash merge(SHA `84ab7744`)했다. 코드·오프라인 작업은 완료했으며 live config·배포·재시작은 BIZ-497 운영 gate 범위다. (2026-07-30)
- [x] **[BIZ-522](mention://issue/87302faa-6047-40a6-8ef6-461add3205ba): 실시간 선수 상태 evidence gate 회귀 고정 및 live 반영** — exact sports evidence 회귀를 [PR #536](https://github.com/ingki3/simpleclaw/pull/536)으로 `dev`에 squash merge(SHA `34bdba5`)하고, [Release PR #538](https://github.com/ingki3/simpleclaw/pull/538)의 merge commit `15a6041` 및 tag `v2026.08.01`로 live 반영했다. live no-send 검증에서 semantic `evidence_required=true`, focused `16 passed`, evidence 없는 no-tool final 차단을 확인했으며, 재시작 후 health 정상·ERROR 0건·Telegram send/edit 0건을 확인했다. (완료: 2026-08-01)

### 2026-07-28

- [x] **[BIZ-511](mention://issue/57a9a033-d881-48c9-b001-f14495d9fc0a): MCP 2.0 FastMCP 호환성 회귀 복구** — `mcp>=1.0,<2`로 v1 runtime 계약을 유지하는 PR #523을 `dev`에 squash merge(`24ffa6b1bff4ff03928de8d560d7b53d14b18d4d`)했고, BIZ-509 PR #522를 새 `dev`와 동기화한 fresh CI에서 Offline Integration·Runtime Contracts·Unit Tests가 모두 통과했다. (2026-07-28)

- [x] **[BIZ-505](mention://issue/cb9f1853-2b50-4f9f-b2a4-75255fe143f0): Fresh structured evidence hard gate 제거** — 일반 final·forced-final을 skill metadata/JSON envelope와 무관하게 보존하도록 post-generation evidence 차단, structured validator, active cron의 `evidence_unavailable` 생성 경로를 제거했다. planner fact-check/realtime lookup, capability routing safety, explicit provider/action failure와 scheduler retry는 유지했다. 검증: targeted 167 passed, 전체 `tests/unit/` 2991 passed·3 xfailed, `ruff check src/ tests/ scripts/` 및 `git diff --check` 통과, PR #518의 `contracts`·`offline-integration`·`pytest` CI green. (2026-07-28)

### 2026-07-19

- [x] **BIZ-460: Gemini OpenAI-compatible A/B 및 LLM 운영 마이그레이션** — `gemini-openai` opt-in profile을 공용 `openai_chat` transport에 등록하고 Google OpenAI-compatible endpoint 예시를 주석 처리된 backend로만 제공했다. profile은 Gemini-native `propertyOrdering`만 복사본에서 제거한다. provider-neutral tool·multimodal attachment·reasoning·native replay는 capability `false`로 선언되어 preflight에서 차단되며, native Gemini route가 이 기능들의 운영 경로로 유지된다. credential-gated integration matrix는 endpoint의 text·TurnAnalysis schema·OpenAI-shaped tool/replay·reasoning·image 입력을 실제로 요청하도록 구성했지만, gate 비활성 시 `5 passed, 6 skipped`이고 billable live matrix는 미실행이다. `openai_responses`는 Chat Completions 재사용을 금지하는 actionable not-registered 확장 계약과 transport 문서로 고정했다. `llm.routes`/backend `transport`/`profile`/credential 변경은 service restart 정책으로 정렬했고, config example·feature/admin 문서에 legacy migration, secret redaction, restart 및 native/default 전환 운영자 승인 gate를 반영했다. 검증: `tests/unit/` 2744 passed·3 xfailed, ruff clean. (2026-07-19)

- [x] **BIZ-454: Gemini TurnAnalysis response_schema 호환성 보강** — BIZ-453 release(#472) 후 live smoke 에서 `TURN_ANALYSIS_RESPONSE_SCHEMA` 의 JSON Schema `additionalProperties` 가 SDK payload 의 `additional_properties` 로 변환되어 Gemini API 가 `400 INVALID_ARGUMENT` (`Unknown name "additional_properties" at generation_config.response_schema`) 를 반환, live config 를 즉시 롤백했던 사고 대응. 수정: `llm/providers/gemini.py` 에 `_sanitize_response_schema()` — Gemini 로 나가는 dict/list schema 복사본에서 `additionalProperties` 를 중첩 object/array 까지 재귀 제거, `propertyOrdering`/`required`/`enum`/`properties`/`items` 등 지원 필드는 보존, 원본 schema 불변(OpenAI-compatible `additionalProperties: false` 계약 BIZ-450 유지), dict 아닌 schema(pydantic 등)는 SDK 변환에 위임. 테스트: `test_gemini_response_schema.py` 신규 7건(root/nested 제거, 원본 불변, 지원 필드 보존, 실제 TurnAnalysis schema, reasoning unset/off/medium-512 조합), `test_turn_analysis_model_selection.py` 에 실제 GeminiProvider 경로 live-like 회귀(sanitized schema + `source=llm` + thinking budget 512) 1건. 검증: `tests/unit/` 2718 passed·3 xfailed, ruff clean, PR #473 CI green → dev squash `a61e19e`, release PR #474 → main merge `52f8b25`. 운영: live checkout ff → 실제 Gemini `gemini-3.5-flash` API smoke(reasoning off/medium-512 모두 `finish_reason=STOP`·schema-valid JSON·`source=llm`) 통과 후 config 백업(`config.yaml.bak-biz454-20260719-175315`)·`agent.turn_analysis` provider=gemini/model=gemini-3.5-flash/max_tokens=2048/reasoning medium-512 재적용·launchd restart·Admin health 200(telegram/dashboard/scheduler true, cron 7). (2026-07-19)

- [x] **BIZ-453: TurnAnalysis 전용 provider/model 설정 및 Gemini 3.5 Flash 전환** — live 대화에서 "금요일에 주말 날씨 확인" 의도를 두 번 정정해도 즉시 날씨 답변으로 축소된 사고 대응: 의도 파악/정규화/route 판단(TurnAnalysis)을 최종 답변 default model(DeepSeek)과 분리해 전용 provider+model 로 지정 가능하게 함. (1) `config_sections/agents.py` — `agent.turn_analysis.provider/model/retry_provider/retry_model/reasoning{enabled,effort,budget_tokens}` 로드/coercion (기존 backend/retry_backend backward-compat 유지, reasoning 기본 off). (2) `llm/router.py` — `ensure_model_backend()`: 기존 백엔드 credentials/구현을 shallow copy 로 재사용하고 model 만 바꾼 가상 백엔드(`base#model`)를 1회 등록·재사용. config 정적 설정 전용 — 사용자/runtime tool 입력 경로에 미노출. (3) `llm/models.py` — `LLMRequest.reasoning` provider-neutral hint 필드. router 는 hint 가 있을 때만 kwargs 로 전달(기존 대역 호환 회귀 0). (4) `llm/providers/gemini.py` — hint 를 native `thinking_config`(budget_tokens 우선, 없으면 effort low/medium/high→256/512/1024)로 매핑, SDK 미지원 시 sanitized 경고 후 no-op degrade, structured output 과 동시 동작. OpenAI(OpenRouter `extra_body.reasoning` 충돌 방지)/Claude/CLI 는 명시적 무시. (5) `orchestrator.py` — `_resolve_turn_analysis_backends()`: 1차 provider+model→backend→default, 재시도 retry_provider+retry_model→retry_backend→llm.fallback (BIZ-452 retry 경로 통합), provider/model attribution 로그(raw-free). (6) `config.yaml.example` — provider=gemini/model=gemini-3.5-flash/reasoning medium 예시. (7) `admin_policy.py` — `agent.turn_analysis.*` Service-restart 정책. 테스트: config 4건, router 가상백엔드/reasoning 전달 8건, gemini thinking 5건, openai 무시 1건, orchestrator/analyze 모델 선택·retry·금요일 주말날씨 reminder regression 7건. 검증: `tests/unit/` 2708 passed·3 xfailed, ruff clean. Out of scope: live config 반영(release 후 운영 단계 — 백업 후 provider/model/reasoning 설정+restart+smoke), llm.default 전환 없음. (2026-07-19)
- [x] **BIZ-452: TurnAnalysis DeepSeek JSON truncation repair 및 Gemini retry** — live trace(83b90b45…)에서 "주가 영향 예측 레포트" follow-up 이 `complex_fact_workflow` 판단(complexity_score=8)까지 나왔는데도 DeepSeek structured JSON 이 tail `reasons` 문자열 중간의 512 출력 cap 에서 잘려 `JSONDecodeError` → conservative fallback 으로 낮게 분기된 사고 대응. 수정: (1) `agent.turn_analysis.max_tokens` 기본값 512→2048 상향 + `retry_backend` config 키 신설(null 이면 `llm.fallback` 위임, `config.yaml.example` 반영). (2) `turn_analysis.py` 에 truncated-tail JSON repair — 문자열/중첩 상태를 추적해 마지막 완결 지점까지 자르고 컨테이너를 닫아 복구, 핵심 필드(route/complexity_score) 이전에서 잘린 payload 는 라우팅을 지어내지 않고 거부. (3) repair 실패 시 fallback backend 로 TurnAnalysis 1회 재시도 후에만 conservative fallback. (4) `OpenAIProvider` send/stream 이 `choice.finish_reason` 을 `LLMResponse.finish_reason`/`diagnostics` 로 보존, structured output + `finish_reason=length` 시 sanitized 경고. (5) 프롬프트/스키마에 `context_summary` 한 문장·`reasons` 최대 3개 짧은 bullet 강제, 파서 clamp(240/3×160자) 추가. 로그는 error_type/raw_len/finish_reason/backend/retry_backend/repair_status 만 — raw 본문 미노출. 테스트: `test_turn_analysis_repair.py` 신규 12건(live-like market follow-up 시나리오 포함), openai structured finish_reason 2건, config 기본값/coercion 3건, orchestrator retry_backend 배선 2건. 검증: `tests/unit/` 2684 passed·3 xfailed, ruff clean. (2026-07-19)
- [x] **BIZ-451: SOUL.md persona config 로딩 검토 및 반영** — check-email cron 알림 반말 사고의 root cause 규명: resolver(`persona/resolver.py`)는 SOUL.md 를 이미 탐색·파싱하지만 assembler(`persona/assembler.py`)의 `_FILE_ORDER` 가 `[AGENT, USER, MEMORY]` 뿐이어서 resolve 된 SOUL 파일이 system prompt 조립에서 조용히 탈락하고 있었다(live config `persona.files` 는 mutation tracker whitelist 용도라 로딩 자체와는 무관). 수정: (1) `_FILE_ORDER` 를 `SOUL → AGENT → USER → MEMORY` 로 확장 — 정체성·말투 규칙이 업무 지시·기억보다 먼저 주입되고, truncation 은 여전히 MEMORY→USER→AGENT 역순이라 SOUL 이 마지막까지 보존됨. (2) config 기본값(`config_sections/agents.py` `_DEFAULTS["files"]`)과 `config.yaml.example` 에 `{name: SOUL.md, type: soul}` 추가(missing 시 resolver warning 후 graceful skip). (3) resolver/models/orchestrator 주석·docstring 정합화. 테스트: assembler SOUL 순서/보존/절삭 3건, resolver SOUL 탐색/graceful skip 2건, config 기본값·example 검증 2건, integration SOUL 파이프라인 1건. 검증: `tests/unit/` 2666 passed·3 xfailed, ruff clean. Out of scope: SOUL.md 본문 수정, check-email recipe tone guard, live config 반영(release 후 배포 단계 — 백업 후 `persona.files` 에 SOUL.md 추가). (2026-07-19)

### 2026-07-16

- [x] **BIZ-444: Vertex AI Gemini provider 지원 검토 및 adapter spike** — Hermes v0.18.0 Vertex provider 대응 spike. **결론: 도입 권고, opt-in 전용 — live 기본 백엔드(`llm.default`) 변경 없음(운영자 승인 사항)**. spike 문서 `docs/spike-vertex-gemini-biz444.md` — 구현 3옵션 비교(google-genai `vertexai=True` 채택 / OpenAI-compatible endpoint·aiplatform SDK 기각), 인증 4경로 우선순위(SA JSON→명시 project+ADC→express api_key→ADC 자동 발견), 토큰 mint/refresh 는 google-auth transport 에 완전 위임(프로바이더 무토큰 상태 계약 — 키 회전이 재시작 없이 반영), 기능 호환성 매트릭스(tool call/structured output/스트리밍/thought_signature 전부 동일 SDK 경로), 운영자 전환 절차. 구현: (1) 신설 `llm/providers/vertex_gemini.py` `VertexGeminiProvider(GeminiProvider)` — 클라이언트 생성만 오버라이드(`genai.Client(vertexai=True, project, location(기본 global), credentials)`), SA JSON `project_id` fallback, 초기화 실패 `LLMAuthError` fail-fast, `GoogleAuthError`(RefreshError 등) isinstance 인증 에러 매핑. (2) `providers/gemini.py` 에러 매핑을 `_map_provider_error()` 훅으로 분리(동작 불변). (3) `router.py` 레지스트리 `vertex_gemini` 등록 + `EXTRA_CONFIG_KEYS` 선언 키(project/location/credentials_path)만 pconf 에서 전달(기존 프로바이더 회귀 0). config 로더 변경 없음(미인식 키 통과). (4) `config.yaml.example` 주석 처리된 redacted 블록. 신규 의존성 0(google-auth 는 google-genai 전이 의존성). 테스트: `test_vertex_gemini_provider.py`(17) 신설. 검증: `tests/unit/` 2626 passed·3 xfailed, ruff clean. Out of scope: live default 전환/deploy(별도 운영자 승인), SA JSON repo 저장 금지 준수. (2026-07-16)

- [x] **BIZ-443: External process·cron·MCP·browser security hardening audit** — Hermes v0.18.0 security round 기준으로 subprocess env scrub·cron exfil·MCP config·cloud metadata SSRF·secret redaction 전반을 audit 하고 발견된 gap 을 봉합. **Audit 결과(이미 안전)**: `skills/executor.py`·`recipes/executor.py`(cron COMMAND 경로)·`agent/command_dispatch.py` 는 `filter_env()` 적용 중, MCP 는 baseline allowlist env, browser_handoff native host 는 내부 URL/password 페이지 차단 + max_chars cap, telegram attachment 는 file_id/size 만 로그(본문 dump 없음), CronJob 모델에 provider/base_url/model override 채널 없음(프로세스 내 `process_cron_message` 로만 LLM 호출). **봉합한 gap**: (1) `agent/builtin_tools.py` — 정규식 SSRF 가드가 놓치던 cloud metadata(`169.254.169.254`, `metadata.google.internal`/`metadata.goog`, Alibaba `100.100.100.200`/CGNAT)·IPv6 link-local/ULA·`.internal`/`.local`/`.localhost` 를 `ipaddress.is_global` 기반 `_is_blocked_url()` 로 차단, `_fetch_static` 은 redirect 자동 추적을 끄고 hop 마다 가드 재검사(공개 URL→내부망 redirect 우회 차단), `_fetch_search_result_body` 도 동일 정책. (2) `security/env_filter.py` — DEFAULT_BLOCKLIST 에 provider prefix(GEMINI/AZURE/OPENROUTER/MISTRAL/COHERE/DEEPSEEK/GROQ/XAI/HF/HUGGINGFACE/CLAUDE)·범용 시크릿 suffix(`*_APIKEY`/`*_CREDENTIALS`/`*_ACCESS_KEY`/`*_PRIVATE_KEY`/`*_PASSWD`)·`ADMIN_API_*` 추가. (3) env 미스크럽 subprocess 3곳 봉합 — `study/collector_adapters.py`·`agent/skill_validate.py` `_smoke`·`proactive/context_provider_resolver.py` 에 `filter_env()` 적용. (4) `skills/mcp_client.py` — `sh -c`/`python -c`/`node --eval` 류 인라인 실행형 stdio 설정을 연결 전에 fail-closed `MCPConnectionError` 로 차단(`_validate_stdio_config`, 해당 서버만 skip). (5) `pyproject.toml` aiohttp floor `>=3.9`→`>=3.12.14`(CVE-2025-53643). 신규 테스트 4파일+MCP 회귀: `test_security_env_filter.py`(7)·`test_skill_executor_env.py`(4)·`test_cron_env_security.py`(3)·`test_web_fetch_security.py`(31)·`test_mcp_client.py`(+5). 검증: `tests/unit/` 2489 passed·3 xfailed, ruff clean. Out of scope: provider credential migration, MCP catalog UX, DNS rebinding 방어(문서화된 한계), agents/spawner·llm/cli_wrapper env(신뢰 경계가 다른 운영자 구성 요소 — audit note 로 기록). (2026-07-16)

### 2026-07-05

- [x] **BIZ-423: Recipe settings timeout을 실행 경로에 반영** — live `/agent-study-daily`가 `settings.timeout: 180`을 선언했는데도 loader가 버려 executor 기본 60초로 실행되다 `Command timed out after 60s`로 실패한 회귀 수정(Option A — in-process 구조 유지 최소 수정). (1) `recipes/models.py`: `RecipeSettings(timeout=60)` dataclass + `RecipeDefinition.settings` 필드, `DEFAULT_STEP_TIMEOUT=60` 상수. (2) `recipes/loader.py`: `settings:` 섹션 파싱 `_parse_settings()` — 양의 int만 채택(bool 제외), 누락/비정상 값은 WARN + 60초 폴백(parse failure로 레시피를 죽이지 않음). (3) `agent/commands.py` 수동 슬래시 v1 `steps:` 경로와 `daemon/scheduler.py` cron v1 경로 모두 `execute_recipe(timeout=getattr(recipe.settings, "timeout", 60))` 전달. (4) `docs/feature-recipes.md`에 `settings.timeout` 사용법 문서화. 단위 테스트 11개 신규(loader 7, slash 2, cron 2) RED→GREEN. 검증: `tests/unit/` 2141 passed(equity live-asset 테스트 1건은 dev에서도 실패하는 기존 이슈), ruff clean, live recipe 파일 smoke — timeout 180 파싱 확인. (2026-07-05)

### 2026-06-30

- [x] **BIZ-388: Agent Study Wiki — Study item SQLite index와 migrations** — Markdown 위키가 source of truth이지만 질문 시 빠른 retrieval/freshness filtering에는 구조화 index가 필요해, 사용자 메모리(`memory_items`)와 섞이지 않게 `study_topics`/`study_items` 테이블을 conversations.db에 additive로 추가했다. (1) migration `db/migrations_data/conversations/0005_agent_study_items.sql`(이슈 SQL draft 그대로 — topics PK·items AUTOINCREMENT·topic/retrieved_at/confidence 인덱스, FK). (2) `study/types.py`에 `StudyItemRecord`(SQLite read model) 추가 — 기존 `StudyItemStatus`(confirmed/reported/rumored/analysis/stale/unknown) 재사용. (3) `study/index_store.py` `StudyIndexStore` — 스키마 SoT를 migration SQL 파일 1곳으로 단일화해 `initialize_schema()`가 동일 SQL을 idempotent 재실행, `upsert_topic`/`add_item`, LIKE 토큰 매칭 `search_lexical`(와일드카드 이스케이프·topic scope), tz-safe `fresh_items`(freshness/valid_until/status/confidence gate). (4) `study/paths.py` `index_path()` 추가 — 운영자 조회(`StudyWikiStore`, BIZ-395)와 같은 `index.sqlite`를 가리키도록 위치 일원화. 단위 테스트 `test_study_index_store.py` 11개 + `test_db_migrations.py` study 테이블·버전 회귀 갱신. 검증: `tests/unit/` 1933 passed, ruff clean. (2026-06-30)

### 2026-06-29

- [x] **BIZ-392: Agent Study Wiki — Daily study runner와 wiki updater** — 매일 공부를 실제로 수행하는 MVP runner를 완성했다. (1) `src/simpleclaw/study/wiki_updater.py`: topic 페이지(Markdown)를 *통째로 덮어쓰지 않고* 관리 섹션("현재 상태"/"최근 업데이트"/"역사적·사회적 맥락"/"형님 관심사와의 연결"/"답변 시 주의사항"/"확인 필요"/"Sources")만 부분 병합하는 `merge_study_update()` — 수동 섹션은 원문 위치 보존, 새 업데이트는 prepend·dedup, 저신뢰(confidence=low)는 "최근 업데이트" 대신 "확인 필요"로 우회 + 면책 문구, `merge_open_questions()`로 open_questions.md 누적. (2) `src/simpleclaw/study/runner.py`: `StudyRunner.run()` 이 topic registry(`topics.yaml`, 아카이브 제외·refresh 우선·관심도 순·`max_topics_per_run` 상한) → source planner(`plan_fetch_requests`) → CollectorRegistry 수집(전부 주입식 fake 대체 가능) → relevance gate(`select_wiki_worthy`) → wiki updater → index store(`index.sqlite study_items`, study_status 호환 스키마) 흐름을 실행하고, `topics.yaml` write-back(last_studied_at/source_count, refresh 플래그 제거)과 `daily/YYYY-MM-DD.md` 데일리 노트("오늘 무엇을 공부했는지" 요약만)를 남긴다. `StudyRunSummary` 불변 결과 반환. (3) `prompts/study/{topic_update,daily_digest}.yaml` 프롬프트 SoT + `load_topic_update_prompt`/`load_daily_digest_prompt` 로더(후속 LLM 요약용). 단위 테스트 23개(`test_study_wiki_updater.py` 11, `test_study_runner.py` 12 — runner가 쓴 index/topics를 운영자 `StudyWikiStore`가 읽는 호환성 포함). 검증: `tests/unit/` 1921 passed, ruff clean. (2026-06-29)
- [x] **BIZ-386: Agent Study Wiki 설계 문서 + config skeleton** — 사용자 메모리(USER.md/MEMORY.md/insights)와 외부 세계 배경지식을 분리하는 경계를 세웠다. `docs/agent-study-wiki.md`(목적/비목표, 메모리 vs Wiki 대비표, 데이터 모델, freshness/confidence·topic 진화 정책), `src/simpleclaw/config_sections/study.py`(`_STUDY_DEFAULTS` + 중첩 재귀 병합 `load_study_config()`, `wiki_dir` Path 정규화), `config.py` facade export, `config.yaml.example` `study:` 예시, `tests/unit/test_study_config.py`(5 케이스). study runner/wiki 생성/retrieval 연동은 후속 이슈(2/11~). 검증: `pytest tests/unit/test_study_config.py` 5 passed, config 회귀 126 passed, `ruff check` clean. (2026-06-29, BIZ-398로 전용 브랜치 분리·PR)

### 2026-06-17

- [x] **BIZ-380: Cron one-shot 알림 메타데이터와 실행 컨텍스트 가드 추가** — cron native tool schema에 `run_once`/`max_runs`/`expires_at` metadata를 노출하고, `handle_cron_action()`이 one-shot metadata를 검증·정규화해 `CronScheduler.add_job()`으로 전달하도록 했다. `process_cron_message()` 경로는 구조적 context flag로 cron mutation(add/remove/enable/disable)을 차단하며 `list`는 유지한다. 회귀 테스트: `tests/unit/test_agent.py -k 'cron'`, `tests/unit/test_tool_schemas.py`, 전체 `tests/unit/` 통과. (2026-06-17)

### 2026-06-13

- [x] **BIZ-368: MCP tools capability gate 추가** — MCP 서버 initialize 결과의 `tools` capability를 확인한 뒤에만 `list_tools()`를 호출하도록 수정해 prompt-only/resource-only 서버를 연결 실패로 오인하지 않게 했다. tools capability가 없는 서버는 connected server로 기록하되 loaded tools 0개로 로그를 남기고, initialize 실패와 tools/list 실패 메시지를 구분한다. 회귀 테스트 3개 추가(`tests/unit/test_mcp_client.py`: prompt-only 미호출 연결, tools capability 도구 등록 유지, tools/list 실패 시 다른 서버 계속 연결) (2026-06-13)
- [x] **BIZ-366: Telegram `/undo N` 대화 컨텍스트 되돌리기** — `messages.deleted_at` soft-delete 마이그레이션(0004) 추가, `ConversationStore.hide_recent_user_turns()` 및 기본 context 조회 숨김 처리(`get_recent`/`get_since`/ID 포함 조회는 감사용 include 옵션 유지), `AgentOrchestrator.process_message()`에서 LLM/tool loop 전에 `/undo`/`/undo N` 선처리. `/undo` 명령 자체는 대화 이력에 저장하지 않고, 잘못된 N/되돌릴 user turn 없음 케이스는 즉시 안내한다. 단위 테스트: store soft-delete/감사 조회/2턴 rewind + orchestrator 명령 파싱/저장 정책/invalid/empty 케이스 추가. 검증: `tests/unit/` 1675 passed, `uvx ruff check src/` passed. (2026-06-13)

### 2026-04-24 ~ 04-25

- [x] **ReAct 패턴 도입** — 멀티턴 스킬 라우터를 Thought→Action→Observation→Answer 루프로 교체
- [x] **레시피 v2 + 슬래시 커맨드** — `/recipe-name`으로 Telegram에서 즉시 실행, `instructions` 필드
- [x] **Dreaming LLM 연동** — 대화 요약을 LLM으로 처리, memory→MEMORY.md, user_insights→USER.md
- [x] **Dreaming 전용 모델 설정** — `daemon.dreaming.model` config 추가
- [x] **Cron NO_NOTIFY** — `[NO_NOTIFY]` 토큰으로 불필요한 알림 생략
- [x] **Cron 격리** — `process_cron_message()` 도입, 대화 히스토리와 분리
- [x] **Workspace 디렉토리** — `.agent/workspace/`에 스킬 파일 출력 격리, `AGENT_WORKSPACE` 환경변수
- [x] **Lazy Loading** — 페르소나, 스킬, 레시피를 매 메시지마다 디스크에서 재로드
- [x] **Smart Python 경로** — `_fix_python_path()`로 스킬 venv 자동 감지
- [x] **Telegram `/` 명령어 수신** — `~filters.COMMAND` 필터 제거
- [x] **Scheduler 리팩토링** — NO_NOTIFY + 알림 콜백을 코어 모듈(`scheduler.py`)로 이동
- [x] **run_bot.py (구 test_telegram.py) thin wrapper화** — 비즈니스 로직을 코어 모듈로 이동
- [x] **AGENT.md 코드 구조 원칙 추가** — 스크립트 비즈니스 로직 금지, 커밋 규칙, lazy loading 원칙
- [x] **호칭 규칙 설정** — AGENT.md에 "형님" 금지, 존댓말 사용 규칙 추가
- [x] **문서 전체 업데이트** — README, PRD, docs/ 11개 파일 최신 반영
- [x] **테스트 전체 통과** — 389개 테스트 (ReAct, 파싱, cron 격리, dreaming 포함)
