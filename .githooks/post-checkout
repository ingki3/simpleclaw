#!/usr/bin/env bash
set -euo pipefail
old_head="${1:-}"
new_head="${2:-HEAD}"
branch_checkout="${3:-0}"
if [[ "$branch_checkout" == "1" ]]; then
  "$(git rev-parse --show-toplevel)/.githooks/graphify-update-if-needed.sh" post-checkout "$old_head" "$new_head"
fi
