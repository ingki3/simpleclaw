/**
 * /system — S13 (System) sub-issue 가 콘텐츠를 채운다.
 */
import { AreaPlaceholder } from "../_components/AreaPlaceholder";

export default function SystemPage() {
  return <AreaPlaceholder path="/system" upcomingIssue="S13" />;
}
