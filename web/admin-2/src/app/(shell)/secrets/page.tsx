/**
 * /secrets — S9 (Secrets) sub-issue 가 콘텐츠를 채운다.
 */
import { AreaPlaceholder } from "../_components/AreaPlaceholder";

export default function SecretsPage() {
  return <AreaPlaceholder path="/secrets" upcomingIssue="S9" />;
}
