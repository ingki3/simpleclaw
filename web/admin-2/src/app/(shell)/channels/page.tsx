/**
 * /channels — S10 (Channels) sub-issue 가 콘텐츠를 채운다.
 */
import { AreaPlaceholder } from "../_components/AreaPlaceholder";

export default function ChannelsPage() {
  return <AreaPlaceholder path="/channels" upcomingIssue="S10" />;
}
