/**
 * /llm-router — S4 (LLM Router) sub-issue 가 콘텐츠를 채운다.
 */
import { AreaPlaceholder } from "../_components/AreaPlaceholder";

export default function LlmRouterPage() {
  return <AreaPlaceholder path="/llm-router" upcomingIssue="S4" />;
}
