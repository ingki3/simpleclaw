/**
 * /cron — S7 (Cron) sub-issue 가 콘텐츠를 채운다.
 */
import { AreaPlaceholder } from "../_components/AreaPlaceholder";

export default function CronPage() {
  return <AreaPlaceholder path="/cron" upcomingIssue="S7" />;
}
