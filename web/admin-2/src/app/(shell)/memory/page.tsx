/**
 * /memory — S8 (Memory) sub-issue 가 콘텐츠를 채운다.
 */
import { AreaPlaceholder } from "../_components/AreaPlaceholder";

export default function MemoryPage() {
  return <AreaPlaceholder path="/memory" upcomingIssue="S8" />;
}
