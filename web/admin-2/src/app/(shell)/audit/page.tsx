/**
 * /audit — S12 (Audit) sub-issue 가 콘텐츠를 채운다.
 */
import { AreaPlaceholder } from "../_components/AreaPlaceholder";

export default function AuditPage() {
  return <AreaPlaceholder path="/audit" upcomingIssue="S12" />;
}
