/**
 * /persona — S5 (Persona) sub-issue 가 콘텐츠를 채운다.
 */
import { AreaPlaceholder } from "../_components/AreaPlaceholder";

export default function PersonaPage() {
  return <AreaPlaceholder path="/persona" upcomingIssue="S5" />;
}
