/**
 * /dashboard — S3 (Dashboard) sub-issue 가 콘텐츠를 채운다.
 */
import { AreaPlaceholder } from "../_components/AreaPlaceholder";

export default function DashboardPage() {
  return <AreaPlaceholder path="/dashboard" upcomingIssue="S3" />;
}
