/**
 * /logging — S11 (Logging) sub-issue 가 콘텐츠를 채운다.
 */
import { AreaPlaceholder } from "../_components/AreaPlaceholder";

export default function LoggingPage() {
  return <AreaPlaceholder path="/logging" upcomingIssue="S11" />;
}
