/**
 * /skills-recipes — S6 (Skills & Recipes) sub-issue 가 콘텐츠를 채운다.
 */
import { AreaPlaceholder } from "../_components/AreaPlaceholder";

export default function SkillsRecipesPage() {
  return <AreaPlaceholder path="/skills-recipes" upcomingIssue="S6" />;
}
