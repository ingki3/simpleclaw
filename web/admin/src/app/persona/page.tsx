import { PlaceholderPage } from "@/components/layout/PlaceholderPage";

export default function PersonaPage() {
  return (
    <PlaceholderPage
      icon="BookText"
      title="페르소나"
      description="AGENT.md / USER.md / MEMORY.md 에디터를 후속 이슈에서 붙입니다."
    />
  );
}
